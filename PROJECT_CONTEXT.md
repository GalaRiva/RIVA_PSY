# PROJECT_CONTEXT.md

Сохранено: 2026-07-22. Цель файла — не пересказывать заново дизайн и ТЗ в каждой новой сессии.

## 0. Важное замечание про расположение проекта

Настоящий Flutter-проект "Rigel PSY" находится **здесь**:
`/Users/galinaegorova/Documents/Rigel_PSY` (git-репозиторий, origin: `github.com/plotnikov228/Rigel_PSY`, branch `update_1.0.8`, последний коммит `0da9690` от 2024-03-17 "update to 1.0.8"). **Это основная рабочая копия — работать дальше нужно именно с ней.**

Папка `~/Desktop/Rigel Psy` (с которой стартовала задача аудита) — это **пустой стандартный Xcode/SwiftUI-шаблон** ("Hello, world!", создан 27.07.2024), не имеет отношения к реальному приложению. Есть также `~/Desktop/Rigel Psy — копия` — дубликат того же пустого шаблона. Не путать эти папки в будущем.

Кодогенерация исходно сделана через **DhiWise** (Figma→Flutter генератор) — этим объясняется нумерация экранов `k1_screen.dart`, `k2_screen.dart`, ... `k70_screen.dart`, которая напрямую соответствует номерам фреймов в Figma.

### 0.1 Полная инвентаризация копий проекта на диске (проверено 2026-07-22)

Найдено 3 git-репозитория с тем же origin (`plotnikov228/Rigel_PSY`) — у всех одинаковый HEAD (`0da9690`, 48 коммитов, ветка `update_1.0.8`), и содержимое `lib/` у всех трёх **побайтово идентично** (проверено `diff -rq`), т.е. рабочих потерь между ними нет:
- `~/Documents/Rigel_PSY` — основная, используется для аудита и дальнейшей работы.
- `~/Downloads/Rigel_PSY` — идентичная копия.
- `~/Documents/Психолог/Rigel_PSY` — идентичная копия (лежит в тематической папке пользователя "Психолог").

Все три репозитория имеют ~458-459 незакоммиченных изменений относительно HEAD (267 добавленных файлов, 92 изменённых, 43 удалённых и т.д.) — то есть **значительная часть текущей функциональности (в т.ч. оба упражнения блока "Обретение") в git не закоммичена вообще**, существует только как рабочее дерево. Раз оно идентично во всех трёх копиях — риска потери нет, но стоит сделать коммит текущего состояния в отдельную ветку до начала миграции, чтобы зафиксировать точку отсчёта.

**Найден четвёртый, действительно расходящийся артефакт**: `~/Downloads/Rigel_PSY (2).zip` (pubspec.lock датирован 30.04.2024 — позже всех трёх рабочих копий от 05.04.2024). Git-история внутри та же (`0da9690`), но **рабочее дерево внутри архива в 11 файлах отличается от `~/Documents/Rigel_PSY` по содержанию**, не только по форматированию:
- `lib/core/models/user_model.dart` — в архиве есть логика **14-дневного триала** (`ORION_TARIFF_14_DAYS`, если с даты регистрации прошло < 14 дней), в текущей рабочей копии эта ветка **отсутствует** (всегда `BASE_TARIFF`).
- `lib/presentation/recomendation/.../desires/widgets/initial_desire_page.dart` — в архиве есть проверка `CurrentUser.tariffIsOrion()` с показом `GoToNewTariffWidget` (пейвол на упражнение "Возвращаем желания" не-Orion пользователям); в текущей копии этой проверки нет.
- `lib/presentation/initial_setup/sign_in/data/repository.dart` — в архиве более полная ветка обработки Google-логина (`createUserResult`/`FirebaseUserResultStatus.WasCreated`), в текущей копии упрощена/обрезана.
- `lib/presentation/main/path/path_final_screen/controller.dart` — в архиве есть доп. кейс `ButtonState.AfterRecording` и другая конфигурация записи звука.
- `lib/presentation/settings/settings_subscription_screen/k13_screen.dart` — в архиве обращение к `CurrentUser.user.currentTariff` и текст "Актуальная подписка"; в текущей — статический текст "Актуальный тариф".
- Остальные 5 файлов (`tariff_model.dart`, `record_add/edit_screen/controller.dart`, `desire_stage_first/second/third_page.dart`) — в основном разница в `dart format`/расстановке `.toUpperCase()`, не логика.

**Важно**: это не односторонняя "архив новее" ситуация — по паре мест (`.toUpperCase()` на текстах в desire_stage_*) как раз в `~/Documents/Rigel_PSY` правки новее, чем в архиве. То есть архив и рабочая копия — это **два независимо разошедшихся незакоммиченных состояния от общего предка** (примерно 05.04.2024), а не строго "старое/новое". Перед миграцией стоит вручную сверить эти 5 содержательных файлов и решить, какая версия логики (триал 14 дней, Orion-пейвол на "Желания", полный Google sign-in flow) должна войти в PWA.

## 1. Продукт

- **Переименование Rigel PSY → RIVA PSY ВЫПОЛНЕНО в коде (2026-07-22)**: bundle ID/applicationId, отображаемое имя, имя Dart-пакета в pubspec.yaml, все текстовые упоминания в UI/уведомлениях/юридических текстах/README, домен в текстах договоров (rigelpsy.ru → rivapsy.com). Подробности, что именно поменяно и что осталось сделать вручную (Firebase-консоль, GitHub-репозиторий, Apple/Google регистрация identifiers) — см. §7 ниже.
- Суть приложения: дневник эмоций по принципу СМЭР (Ситуация-Мысли-Эмоции-Реакция), КПТ-упражнения, аудио-рекомендации по группам эмоций, графики/аналитика состояния, платная подписка.

## 2. Дизайн (Figma)

Ссылка: https://www.figma.com/design/7pHPT7ZulHAdniQ1akCE8P/Riva-PSY--Base-
Файл: **Riva PSY (Base)**. Node проверенный: `3111:40` (страница "Приложение 360х812 Главная июль 2026").

Примечания по доступу:
- Figma MCP-инструменты (`get_design_context` / `get_metadata`) требуют открытый десктоп-клиент Figma с выделенным слоем — в headless-режиме не работают ("nothing selected").
- `get_screenshot` по fileKey+nodeId работает без десктоп-клиента — им и пользовались для просмотра.
- Просмотр через встроенный браузер (figma.com в режиме "view only" без логина) на этой сессии зависал / не давал живого рендера — не полагаться на него, использовать `get_screenshot`.

Что видно на странице (карта фреймов, сверху вниз/слева направо):
- **Онбординг**: сплэш с лого RIVA → регистрация/вход (email, телефон, Google/Apple) → пароль/подтверждение → согласия (чек-боксы) → напоминания (частота уведомлений) → предложение тарифа → запрос push-разрешения.
- **Настройки**: список пунктов меню, профиль, пароль (сколер + круговой индикатор), подписка/тариф, промокод.
- **Основной путь** (большой кластер вертикальных экранов): Что произошло / Где / С кем / Какую эмоцию (позитив/негатив/нейтрал табы) / Доп. эмоции / Что с телом (сетка иконок + силуэт тела спереди/сзади) / Что делал / Что подумал / Экран с круговым индикатором интенсивности эмоции.
- **Рекомендации и упражнения**: табы Введение / Медитации / Негативные / Группа Паника, для каждой из 10 групп негативных эмоций (Злость, Страх, Грусть, Обида, Неуверенность, ...) — список аудио/текстовых карточек.
- **"Обретение" (второй блок упражнений)**: Упражнение 1 "Отработать иррациональное" (карточки с фото, работа с мыслью/действием, счётчик прогресса), Упражнение 2 "Престиж в фокусе" (позитивный путь, красивые лайфстайл-фото, супер-задание "Три дня без жалоб" с экранами День 1/2/3).
- **Желания** ("Возвращаем желания"): экран с таймлайном/точками и аватарками, отчёт "Исполняем желания".
- Календарь выбора периода — отдельный переиспользуемый компонент.

Дизайн по составу экранов **опережает или как минимум совпадает** с реализованным кодом почти везде, кроме Упражнения 3 ("О деньгах, отношениях... отработка негативных установок") — под него в просмотренной части страницы явных фреймов не встретилось (либо не создан, либо в другом месте файла).

## 3. ТЗ (Google Doc)

Ссылка: https://docs.google.com/document/d/1MPfe7CbZgunSL1OyMwEsROEzTdzqFmPsEyhHivdsSCY/edit

Документ большой (~49k символов), русскоязычный, написан как смесь спецификации и черновика/переписки (внутри есть блок явно в диалоговом стиле "Рекомендация для тебя..." — то есть в сам ТЗ вклеен фрагмент чата с рекомендацией по архитектуре оплаты). Структура:

1. **Приложения-ссылки**: "Рекомендации проживания" (Google Doc), таблица "Что/Где/С кем/Тело/Эмоции" (Google Sheet), папки с аудио (несколько, включая "новое взамен старого" и отдельно "Депрессия"), юридические документы (Условия использования, Политика конфиденциальности).
2. **Общие технические требования**:
   - Кроссплатформенное приложение (⚠️ в контексте PWA-перехода этот пункт закрывается самим фактом перехода на PWA).
   - Хранилище аудио/материалов — **Cloudflare R2** (в коде сейчас Firebase Storage — R2 не внедрён).
   - Оплата **на собственном сайте** (не через сторы) — см. п.5 ниже, это ключевой архитектурный момент.
   - Локализация: **ru / en / es**, при смене языка меняется и набор аудио под язык.
   - Адаптивные изображения, скролл если текст не помещается.
   - Переименование Rigelpsy → Rivapsy (отдельная задача).
   - Встроенная простая система автообновлений.
3. **Иерархия эмоций**: 3 верхнеуровневые группы (позитивные/негативные/нейтральные) → родственные подгруппы (напр. "Злость": возмущение, негодование, раздражение) → конкретные эмоции. Нейтральные при фиксации распределяются в "Обычное+" или "Обычное-" в зависимости от знака.
4. **Онбординг**: загрузка, лого, регистрация (1 раз на установку) с чекбоксами согласий, всплывающие экраны про напоминания/тариф/push, юридические тексты хранятся в Настройках, не показываются целиком на экране согласия.
   - Открытый вопрос из ТЗ: согласие разовое vs. повторный запрос при смене законодательства — решение не зафиксировано.
5. **Главный экран**: круговой сенсорный регулятор эмоционального состояния (20 делений, 10 позитив/10 негатив, 0 = "Обычное"), кнопки Сохранить / Пройти путь, меню (Основной путь, Графики, Настройки, Записи), быстрая кнопка "Справиться с эмоцией сейчас".
6. **Основной путь** — пошаговый ввод: Что произошло → Где → С кем → Какую эмоцию (+ доп. эмоции, подгрузка связанных тегов по группе) → уровень интенсивности (шкала 1-10) → Что с телом (часть тела + ощущение, можно несколько) → Что автоматически сделал (текст) → Что подумал (текст) → упражнения/аудио-рекомендации по выбранной эмоции → кнопка "Выговориться" (диктофон, запись хранится локально). Все шаги поддерживают "Добавить своё" и одиночный/двойной тап для выбора/отмены тега. Строка поиска по тегам на каждом экране.
7. **Графики и записи**: архив записей (поиск, добавление, редактирование), календарь. 7 типов графиков:
   1. Диагностика состояния (линейный график по времени, бесплатно)
   2. СМЭР сводный табличный PDF-отчёт (бесплатно, экспорт email/мессенджеры/устройство)
   3. Какие эмоции испытываю (круговая диаграмма %, бесплатно)
   4. Позитивные vs негативные эмоции (круговая диаграмма, бесплатно)
   5. Где в теле живут эмоции (+ 5.1 позитивные, 5.2 негативные раздельно) — визуализация на теле
   6. Детализация Эмоция→Часть тела
   7. Где и какие эмоции по местам
   Остальные графики (не 1-4) — только в платном тарифе.
8. **Рекомендации и упражнения**: 10 групп негативных эмоций + отдельно "Общие рекомендации", "Паника", "Аффект" (эти три — бесплатные вместе с "Введение"). Позитивные эмоции только в аналитике, не в разделе рекомендаций.
9. **Сбор обратной связи**: текстовое поле + прикрепление скриншота; при баг-репорте — автозагрузка версии приложения, device info, статус iCloud backup.
10. **Напоминания**: первый месяц — 2 раза в день, без возможности отключить; далее гибкая настройка частоты. Плюс программируемые push-уведомления.
11. **Пароль**: 4-значный пин, включение/выключение сколером в Настройках.
12. **Платежи — ЗАФИКСИРОВАНО РЕШЕНИЕ (2026-07-22): используем только новую схему.**

    В самом ТЗ было два конфликтующих описания оплаты. Решение пользователя: **старая модель считается deprecated и не переносится на PWA.**

    - **~~Старая модель (DEPRECATED, не переносим)~~**: тарифы "Бесплатный"/"Полный доступ", месяц 9€/год 65€, промокоды, автопродление, оплата через Yookassa + In-App Purchase. Уже реализована в текущем Flutter-коде (`yookassa_payments_flutter`, `in_app_purchase`, `settings_buy_subscription_screen`, `settings_payment_subscription_screen`, `settings_tariff_activated_screen`, `tariff_model.dart`), но при переезде на PWA этот код и его бизнес-логику **не переносим и не используем как образец** — сама модель (App Store/Google Play IAP) физически неприменима к PWA.
    - **✅ Актуальная модель (единственная, для PWA)**:
      1. Оплата происходит **на сайте через Stripe Connect**, не в самом приложении/PWA.
      2. **До оплаты** — полностью free-тариф без регистрации: локальный анонимный `device_uuid` генерируется на клиенте без обращения к серверу; весь прогресс/избранное/история хранятся **только на устройстве** (в PWA — IndexedDB/localStorage, аналог Hive); сервер не получает никаких данных о бесплатном пользователе.
      3. **Регистрация происходит только в момент оплаты**: пользователь вводит email на сайте → passwordless / magic link (без пароля) → создаётся `user_id`, привязанный к Stripe Customer ID.
      4. Оплата подтверждается через Stripe webhooks (`checkout.session.completed`, `invoice.paid`, `customer.subscription.updated/deleted`) → backend обновляет статус подписки для `user_id`.
      5. После оплаты сайт показывает кнопку "Открыть в приложении" → **deep link / universal link** с одноразовым токеном → автологин в PWA, выдаётся JWT.
      6. **Миграция локальных данных на сервер**: однократно, локальные данные (привязанные к `device_uuid`) отправляются на сервер и привязываются к `user_id`; конфликты (повреждённые/задвоенные данные) разрешаются в пользу более полной/новой версии, лог конфликта сохраняется для отладки. После миграции `device_uuid` перестаёт быть основным идентификатором.
      7. Дальше приложение при каждом запросе проверяет статус подписки через собственный backend API (JWT-авторизация, не через Apple/Google). Синхронизация между устройствами — только для оплативших/зарегистрированных.
      8. Данные на сервере — минимальный набор: email, статус подписки, прогресс/контент-данные, привязанные к `user_id`, без лишних персональных полей.
    - **Технические зависимости новой модели** (ничего из этого в коде пока нет — строить с нуля под PWA): backend-эндпоинт приёма Stripe webhook; API проверки статуса подписки (JWT); эндпоинт миграции локальных данных (принимает `device_uuid` + payload, возвращает `user_id`); deep link/universal link конфигурация для автологина (на вебе это существенно проще — обычный URL + service worker, без нативных Universal Links/App Links).
    - **Открытые вопросы, не закрытые ни в ТЗ, ни в решении пользователя** — учитывать при реализации:
      - Что происходит с локальными free-данными, если пользователь теряет устройство/очищает браузер до оплаты (ожидаемая потеря, но нужно UX-предупреждение).
      - Нужен ли отдельный endpoint для GDPR right to erasure (полное удаление `user_id` и связанных данных по запросу).
      - Формат и защита одноразового токена в deep link (TTL, защита от повторного использования).
    - **❌ Пробный период (trial) — РЕШЕНИЕ (2026-07-22): НЕ добавляем.** В архиве `~/Downloads/Rigel_PSY (2).zip` был найден незакоммиченный код 14-дневного триала (`TariffModel.ORION_TARIFF_14_DAYS`, автоматически назначаемого в `user_model.dart` и при регистрации в `sign_in/data/repository.dart`) — этот код **сознательно не переносится и не используется как образец**. Модель в PWA простая и без промежуточных состояний: оплатил на сайте → доступ открыт, не оплатил → free-тариф. Апелляций/грейс-периодов на этом этапе нет.

    ### Правило гейтинга контента free/paid — фиксация для проверки статуса подписки в PWA (2026-07-22)

    Единственный источник истины по тому, что доступно бесплатно, а что закрыто — ниже. Проверяется на бэкенде через статус подписки по `user_id` (см. новую модель выше), не на клиенте.

    **Открыто на free (бесплатном) тарифе:**
    - Аудио/текстовые рекомендации только из групп: **Введение**, **Паника**, **Аффект**.
    - Графики: **График 1** (Диагностика состояния), **График 2** (СМЭР сводный табличный PDF-отчёт), **График 3** (Какие эмоции испытываю), **График 4** (Позитивные vs негативные эмоции).
    - Весь "Основной путь" (ввод СМЭР-записей) — не является платным контентом сам по себе, доступен всем.

    **Закрыто, доступно только на платном тарифе:**
    - Аудио/текстовые рекомендации всех 10 групп негативных эмоций, кроме Паники и Аффекта.
    - Графики 5, 5.1, 5.2, 6, 7 (тело/места/детализация).
    - Весь блок "Обретение" целиком:
      - Упражнение 1 "Отработать иррациональное".
      - Упражнение 2 "Престиж в фокусе — счастье" (включая супер-задание "Три дня без жалоб").
      - Упражнение 3 "О деньгах, отношениях и не только" (когда будет реализовано).
      - Упражнение 4 "Возвращаем желания".
    - Принцип пейвола, а не конкретный Dart-код из архива: показывать экран-заглушку с предложением оформить подписку вместо содержимого экрана, если пользователь не оплатил — переносится как UX-паттерн (`GoToNewTariffWidget` из архива — только как референс идеи, не как код для копирования).
13. **Аудио-обновления**: добавить новые записи (Вина, Зависть — с указанием расположения в меню), заменить несколько существующих (Регулятор громкости, Аффект 1, Аффект 2).
14. **"Второе обновление — Обретение"** (уже частично закодировано, см. §4):
    - Упражнение 1 "Отработать иррациональное": берёт мысль+действие из уже заполненной записи Пути (только негативные эмоции), пользователь оспаривает и формулирует альтернативу для мысли и для действия, ведётся счётчик прогресса (первые 7 дней), итоговый отчёт-таблица (3 столбика мысль, 3 столбика действие) с экспортом в PDF. Доступно только на платном тарифе.
    - Упражнение 2 "Престиж в фокусе — счастье": позитивный путь (без мыслей/действий, только Что/Где/С кем/Эмоция/Тело), запись текста приятного момента дня, серия экранов-подкреплений после 1-5 прохождений в день, разблокировка "Супер задания" после 100 позитивных записей → супер-задание "Три дня без жалоб" (по дням, с чек-боксом выполнения).
    - Упражнение 3 "О деньгах, отношениях и не только" (отработка негативных убеждений): пользователь пишет ограничивающее убеждение → выбирает вызываемые им эмоции (с интенсивностью) → подтверждает готовность менять → формулирует желаемые эмоции взамен → формулирует ценность каждой → супер-задание — аффирмации. **В коде не найдено соответствующих моделей/экранов — не начато.**
    - Упражнение 4 "Возвращаем желания": лёгкие желания "здесь и сейчас", детализация желания, экран "могу исполнить сейчас/в течение 30 минут", если нет — планирование даты, итоговый отчёт-таблица желаний со статусами (дата/удовлетворено/запланировано/пропало).
15. Публикация в сторах — открытый вопрос в самом ТЗ ("можно ли установить/скачать на сайте собственном?") — фактически TЗ сам admits неопределённость модели дистрибуции, что дополнительно поддерживает переход на PWA.

## 4. Состояние кода на момент аудита (Flutter, `lib/`, 366 файлов)

Стек: Flutter (Dart SDK >=2.12.0), GetX + flutter_bloc + provider (смешанный state management), Firebase (Auth/Firestore/Storage/Messaging/Analytics), Hive + SharedPreferences (локальное хранилище), easy_localization, Yookassa + in_app_purchase (оплата), just_audio/flutter_sound (аудио), syncfusion_flutter_charts + pie_chart + sleek_circular_slider (графики/регулятор), retrofit/dio (сеть), freezed (immutable-модели), pdf/printing (экспорт отчётов). Сгенерирован изначально через DhiWise (Figma→код), сильно доработан вручную впоследствии (git-история с 2023-05 по 2024-03, ~150+ коммитов).

**Полностью/почти полностью реализовано** (по составу экранов и виджетов совпадает с ТЗ):
- Онбординг: сплэш, регистрация/вход (email, телефон, Google, Apple), сброс пароля, верификация.
- Настройки: профиль, пароль (PIN), напоминания, подписка/тариф/промокод, О приложении, языки.
- Главный экран: круговой регулятор (`circular_slider_widget.dart`, `sleek_circular_slider`).
- Основной путь целиком: Что произошло → Где → С кем → Эмоция (позитив/негатив/нейтрал табы, доп. эмоции) → Тело (силуэт, части тела) → Что делал → Что подумал → упражнения/аудио + кнопка "Выговориться" (диктофон).
- Записи: архив, поиск, добавление, редактирование, календарь.
- Графики: отдельные виджеты на большинство из 7 графиков ТЗ (`diagnosis_of_the_condition_widget`, `report_widget` + PDF, `what_emotion_widget`, `where_in_body_widget`, `where_and_what_emotions_widget`).
- Рекомендации: табы (Введение/Медитации/Негативные/Паника/Аффект), 10 групп негативных эмоций (`negative_emotion_tabs`).
- "Обретение" Упражнение 1 (`working_out_irrational/*`: alternative, challenge, gratitude, record, count_bar, relax_dialog) и Упражнение 2 (`happiness_in_focus/*`, `super_quest/*` — совпадает с "Три дня без жалоб").
- Желания (Упражнение 4): `desires/*` (bloc, диалоги, отчёт).
- Локализация: `ru-RU.json`, `es-ES.json` есть как файлы, но в `main.dart` реально включена **только русская локаль** (испанская закомментирована), английской локали нет вообще.

**Частично / нужно проверять глубже**:
- Сбор обратной связи / баг-репорт (`settings_errors_screen`, `settings_offers_screen`) — экраны есть, но не подтверждено, что реализован именно автозахват скриншота + device info + статус iCloud backup, как требует ТЗ.
- Юридические согласия при регистрации (чекбоксы) — есть поля в форме регистрации, но не проверялось соответствие текстов и логика "не показывать полный текст, только в Настройках".

**Не начато / отсутствует**:
- Упражнение 3 "О деньгах, отношениях..." — файлов/моделей не найдено.
- Актуальная (единственная теперь) архитектура платежей — Stripe Connect, device_uuid, JWT, deep-link автологин, миграция локальных данных на сервер — **полностью отсутствует**, сейчас в коде только deprecated Yookassa/IAP-модель (см. §3 п.12), которую переносить не будем.
- Cloudflare R2 для хранения аудио/материалов — сейчас Firebase Storage.
- Английская локализация.
- Система автообновлений.

**Модуль "Pills" — РЕШЕНИЕ (2026-07-22): остаётся в проекте, фича нужна.**

Полноценный трекер приёма лекарств/медикаментов, никак не описанный в прочитанной части ТЗ (ТЗ целиком про эмоциональный СМЭР-дневник), но принят как часть продукта. Состав:
- `Настройки → "Напоминания о приёме"` (`settings_pills_screen.dart`): список назначенных препаратов с двумя вкладками — "Актуальные назначения" и "По дате внесения"; кнопка "Добавить препарат".
- `PillModel` (`pill_model.dart`): название препарата, список часов приёма в течение дня (`hoursOfTakingPills`), дата начала/окончания курса, дата создания записи, список `adoptions` (факты приёма, см. `adoption_model.dart`); есть метод `actual()` — курс активен, если сегодня между start/end датами.
- `settings_pills_add_bottom_sheet` / `settings_pills_edit_bottom_sheet` — добавление/редактирование препарата снизу экрана (bottom sheet).
- `settings_pills_calendar` — календарь приёма препарата (переиспользует общий `CalendarController`).
- `ConcretePillScreen` (лежит в `presentation/charts/concrete_pill/`, а не в settings — видимо открывается по push-уведомлению-напоминанию): всплывающий экран с именем препарата, временем и датой приёма — то есть "пора принять лекарство X".
- `pill_reminders_screen.dart` (`presentation/initial_setup/pill_reminders/`) — есть отдельный экран настройки напоминаний на этапе онбординга.

Фича самодостаточная и не пересекается по данным с основным СМЭР-функционалом (собственная модель, собственные экраны, отдельная ветка в Настройках и в push-уведомлениях).

### Оценка качества текущей реализации (разобрано 2026-07-22)

Функционально модуль закрывает весь цикл (добавить препарат → назначить время/период приёма → напоминание-попап → список актуальных/по дате → редактирование), и визуально консистентен с остальным приложением (переиспользует `CustomButton`, `CustomTextFormField`, `AppStyle`, `CustomAppBar`). Но по архитектуре и аккуратности кода это заметно наименее доработанная часть кодовой базы:

- **Хранение данных рискованное**: `PillsRepo.updateEvent()` при каждом сохранении **удаляет всю Hive-таблицу и переписывает её заново** поэлементно (`deleteBox` → цикл `setBox`), вместо точечного добавления/обновления одной записи. Если процесс прервётся между удалением и повторной записью — можно потерять все препараты целиком, не только изменяемый.
- **Чтение с побочным эффектом записи**: `PillsController.getPills()` — по сути геттер, но в конце вызывает `repo.updateEvent(pills)`, то есть каждое открытие экрана перезаписывает всё хранилище целиком. Нарушение разделения "чтение/запись", лишняя нагрузка и лишний риск потери данных на пустом месте.
- **Нет обработки ошибок**: чтение из Hive (`jsonDecode` на каждую запись) ничем не защищено — одна повреждённая запись уронет весь экран списка препаратов. Единственный `try/catch` в модуле (`setDurationOfReception`) просто делает `print()` и молча проглатывает ошибку, без обратной связи пользователю.
- **Дублирующая и рассинхронизированная логика**: метод `checkDateInRange` объявлен в двух местах (`PillsController` и `PillsBottomSheetController`) с **разной семантикой** — в одном месте окно ±1 день от даты начала курса, в другом — честная проверка "между start и end". Одинаковое имя, разное поведение — источник будущих багов.
- **Валидация времени приёма кустарная**: часы/минуты разбираются вручную по индексам строки (`text[0]+text[1]`), верхняя граница проверяется как "часы < 24" и "минуты < 60" вместо корректных 23/59 — на границе возможны некорректные значения времени.
- **"Мёртвый" код валидации формы**: в `PillsAddBottomSheet` объявлен `formKey = GlobalKey<FormState>()`, но никогда не оборачивается в `Form` и не вызывается `validate()` — то есть `validator` у текстового поля с названием препарата **фактически никогда не срабатывает**, хотя выглядит так, будто валидация есть.
- **Смешаны два UX-паттерна валидации в одном экране**: для названия препарата — обычный inline-error у текстового поля (`CustomTextFormField.validator`, но нерабочий, см. выше), а для времени приёма — модальное окно с сообщением об ошибке через смену цвета рамки на красный. Две разные идиомы там, где могла бы быть одна.
- **Не убранные следы кодогенерации**: пустые `// TODO: implement onInit`, `// TODO: implement updateTasks` (комментарии остались от исходного DhiWise-шаблона, реализация под ними уже есть, но комментарий не удалён), задвоенный `import` в `pills_list_model.dart` — по мелочи, но показывает, что модуль не получил той же полировки, что и более новые части (например "Обретение").
- **Архитектурно самый "старый" слой**: чистый GetX с ручными вызовами `update()` по всему коду — паттерн более раннего поколения приложения. Более новые фичи ("Обретение": `working_out_irrational`, `happiness_in_focus`, `desires`) уже написаны на `flutter_bloc`/Cubit. Pills остался на старом подходе и не мигрировал вместе с остальными.

**Что стоит сделать иначе при переносе на PWA** (не просто 1:1 переписать, а поправить по ходу):
1. Инкрементальное хранение (добавить/обновить/удалить одну запись в IndexedDB), без полной перезаписи всей коллекции на каждое действие.
2. Убрать побочный эффект записи из операции чтения списка препаратов.
3. Добавить реальную обработку ошибок чтения (устойчивость к повреждённой записи) с понятной обратной связью пользователю вместо silent-fail.
4. Свести валидацию времени к одной корректной реализации (0-23 / 0-59) и к одной UX-идиоме (либо везде inline-ошибки в форме, либо везде модалка — не вперемешку).
5. Либо реально подключить `Form`/`formKey` и `validate()`, либо убрать эту неработающую обвязку, чтобы не создавать ложное ощущение проверки.
6. Убрать дублирующийся `checkDateInRange` с разной семантикой, оставить одну корректную версию.
7. Реализовать состояние на том же стеке, что и остальной PWA (не тащить GetX-паттерн как единственно ради "было так во Flutter") — иначе Pills снова останется архитектурным аутсайдером в новом кодовом стеке.
8. Механизм напоминаний в любом случае переписывается с нуля под веб (Push API + Service Worker вместо `workmanager`) — это уже входит в общий план миграции, но именно в Pills напоминание вызывается напрямую (`WorkManagerService().initService()` из `addPill`), так что стоит сразу закладывать архитектуру напоминаний с учётом и этого модуля, а не только основного дневника.

## 5. Что переносимо / что переписывать заново для PWA

**Переносимо как концепция / 1:1 переиспользуемо в виде спецификации** (не как код, а как источник истины по неймингу и структуре):
- Иерархия сущностей и их названия: эмоции (группы/подгруппы/конкретные эмоции), места, персоны, части тела, записи (СМЭР), тарифы — модели данных (`*_model.dart`, `freezed`-классы) читаемы как спецификация полей даже если Dart-код не используется напрямую.
- Разбиение на экраны и порядок навигации Основного пути — это чистый UX-флоу, воспроизводимый в любом фреймворке.
- Структура графиков (7 типов, что откуда берут данные) — бизнес-логика агрегации переносима как алгоритм (пересчитать на TS/SQL/что угодно).
- Дизайн-токены: `app_style.dart` / `app_decoration.dart` (566+370 строк) — цвета, текстовые стили, отступы, скорее всего экспортируемы в CSS-переменные / Tailwind-конфиг вручную (не автоматически, но как источник).
- Тексты, копирайтинг (все "тёплые" сообщения-подбадривания из ТЗ и Figma) — переносятся как есть.
- Названия роутов/экранов (`k1`...`k70`) — полезны как чек-лист "что вообще нужно построить", по одному экрану на пункт.

**Нужно писать заново (Flutter-специфика, не переносится на веб напрямую)**:
- Вся Flutter Widget-разметка (`*_screen.dart`, `*_widget.dart`) — просто UI-код, придётся верстать заново под веб-стек (React/Vue/etc.).
- GetX/flutter_bloc/provider — состояние управления, замена на веб-стейт-менеджер (Redux/Zustand/Context/etc.).
- Hive/SharedPreferences — заменяются на IndexedDB/localStorage/веб-совместимое хранилище.
- Firebase Auth платформенные SDK (Google/Apple sign-in нативные потоки) — на вебе другой OAuth-флоу (Firebase Web SDK или отдельный auth-провайдер).
- just_audio/flutter_sound/record (запись голоса) — на вебе через MediaRecorder API/Web Audio API, другая реализация.
- awesome_notifications/flutter_local_notifications — на вебе Push API + Service Worker (принципиально другая модель, тем более для PWA это ключевая история).
- workmanager (фоновые задачи) — на вебе через Service Worker/Background Sync, сильно ограниченные возможности по сравнению с мобильной ОС.
- yookassa_payments_flutter/in_app_purchase — не нужны вовсе при новой (веб) модели оплаты из ТЗ; строить Stripe-интеграцию с нуля под веб — что фактически совпадает с тем, что и так предписывает актуальная версия ТЗ.
- pdf/printing (генерация PDF) — на вебе другой инструмент (напр. jsPDF/Puppeteer/серверная генерация).
- circular_slider/sleek_circular_slider (кастомный тач-регулятор) — придётся реализовать заново на канвасе/SVG под веб-ввод (мышь+тач).
- syncfusion_flutter_charts/pie_chart — заменить на веб-чартинг библиотеку (Recharts/D3/Chart.js/etc.), логика графиков (что и как считать) переносится, рендеринг — нет.
- Deep link/Universal Links механизм — на PWA заменяется обычными URL + service worker, сильно проще технически.
- Вся нативная обвязка (Podfile, GoogleService-Info.plist, android/ каталог, xcodeproj) — не нужна для PWA вообще.

## 6. Открытые несоответствия, требующие решения до/во время миграции

1. ~~Платёжная модель в ТЗ — два конфликтующих описания~~ — **РЕШЕНО 2026-07-22**, см. §3 п.12: используем только новую (сайт + Stripe + email + device_uuid + JWT), старую (Yookassa/IAP) считаем deprecated и не переносим.
2. Английская локализация заявлена в ТЗ, но не реализована даже файлом переводов (только ru/es файлы, и даже es отключена в коде). — Отложено пользователем, вернуться позже.
3. Хранилище аудио — заявлено Cloudflare R2, в коде — Firebase Storage; нужно решение, мигрировать ли данные или тз устарел. — Отложено пользователем, вернуться позже.
4. Упражнение 3 не имеет кода вовсе — нужно либо посчитать это фичей v2 после PWA-релиза, либо включить в объём миграции с нуля. — Отложено пользователем, вернуться позже.
5. ~~Модуль "Pills"~~ — **РЕШЕНО 2026-07-22**: остаётся в проекте, нужен. Оценка качества реализации и список доработок для PWA — см. §4 (раздел "Оценка качества текущей реализации").
6. Разночтение в самом ТЗ: юридическая часть про повторный запрос согласий при смене законодательства помечена как открытый вопрос — решения нет ни в ТЗ, ни в коде.
7. Система автообновлений заявлена в ТЗ, в коде не найдена. — Отложено пользователем, вернуться позже.
8. ~~Ничего из текущей функциональности не закоммичено в git~~ — **ЗАКРЫТО 2026-07-22**: текущее рабочее дерево `~/Documents/Rigel_PSY` (458 файлов из 459 — без `.DS_Store`/собранного `.aab`) закоммичено в новую ветку `pre-pwa-migration-snapshot` (коммит `2bf3c2a`, поверх `0da9690`). Ветка `update_1.0.8` не тронута. Дальше в работе — только `~/Documents/Rigel_PSY`.
9. ~~Обнаружен разошедшийся архив `~/Downloads/Rigel_PSY (2).zip`~~ — **РЕШЕНО 2026-07-22**: код из архива не переносим.
   - 14-дневный триал (`ORION_TARIFF_14_DAYS` в `user_model.dart`/`sign_in/repository.dart`) — **не добавляем**, в новой платёжной модели trial-периода нет (см. §3 п.12).
   - Пейвол на "Возвращаем желания" — переносим только **принцип** (гейтинг закрытого контента заглушкой с предложением подписки), не Dart-код из архива; полное правило гейтинга free/paid — см. §3 п.12 ("Правило гейтинга контента").
   - Отступ в `sign_in/data/repository.dart` и пустой `case` в `path_final_screen/controller.dart` — не трогаем, будет переписано заново при переносе на веб.
   - Сам архив `~/Downloads/Rigel_PSY (2).zip` — оставлен на диске как есть, в работе как источник кода больше не используется.

## 7. Переименование Rigel PSY → RIVA PSY (выполнено 2026-07-22)

Сделано полностью в коде `~/Documents/Rigel_PSY` (226 файлов). Изменения пока не закоммичены — лежат поверх ветки `pre-pwa-migration-snapshot`.

**Новые идентификаторы:**
- iOS `PRODUCT_BUNDLE_IDENTIFIER`: `com.rigel.psy` → `com.riva.psy` (и RunnerTests: → `com.riva.psy.RunnerTests`, попутно поправлена устаревшая заглушка `com.example.listenmebaby71SApplication17.RunnerTests`, которая осталась от исходного DhiWise-шаблона).
- Android `applicationId`: `com.rigel_psy.app` → `com.riva_psy.app`; Kotlin-пакет `MainActivity.kt` перемещён из `com/listenmebabysapplication/app/` (не совпадал с applicationId — старая нестыковка) в `com/riva_psy/app/`.
- Отображаемое имя (`CFBundleDisplayName`, `android:label`, заголовки экранов/уведомлений): "Rigel PSY" → "RIVA PSY".
- `CFBundleName`/внутреннее имя: `rigel-psy` → `riva-psy`.
- Dart package (pubspec.yaml `name:`, все импорты в `lib/`/`test/`): `listenmebaby71_s_application17` → `riva_psy` (544 вхождения в 207 файлах).
- Apple Pay merchant ID: `merchant.com.rigel.psy` → `merchant.com.riva.psy` (в `Runner.entitlements` и `payment_configuration.dart`).
- Sign in with Apple `clientId` (Services ID): `com.rigel.app` → `com.riva.app` (`services_auth_service.dart`).
- Домен/email в юридических текстах (Условия использования, Политика конфиденциальности — `about_app_screen/controller.dart`, `ru-RU.json`, `es-ES.json`) и в PDF-экспортах (`alternative_pdf.dart`, `report_model.dart`): `rigelpsy.ru` / `support@rigelpsy.ru` → **`rivapsy.com`** / `support@rivapsy.com` (по решению пользователя).
- Все текстовые бренд-упоминания ("Rigel PSY", "RIGEL PSY", "Rigel Psy" и т.п.) в UI, пуш-уведомлениях, README, backup-именах файлов (`rigel_psy_backup_from_` → `riva_psy_backup_from_`) — нормализованы к "RIVA PSY".
- Символы в `image_constant.dart`: `imgRigel` → `imgRiva`, `imgRigelCyan700` → `imgRivaCyan700` (сами SVG-файлы `img_rigel.svg`, `img_rigel_cyan_700.svg`, `rigel_small_logo.svg` **не переименованы и не тронуты** — по инструкции оставить иконки как есть).

**Сознательно НЕ тронуто (нужно ручное действие вне кода):**
- `ios/GoogleService-Info.plist`, `ios/Runner/GoogleService-Info.plist`, `android/app/google-services.json` — содержат `bundle_id`/`package_name`/`project_id`/`storage_bucket` со старым именем (`rigel-psy`). Это файлы, сгенерированные Firebase-консолью — их нельзя просто отредактировать руками, они должны быть перевыпущены после того, как в Firebase Console будет создана/переименована регистрация приложения под новыми `com.riva.psy` (iOS) и `com.riva_psy.app` (Android). До этого момента Firebase Auth/Storage/Messaging/Analytics будут падать или работать некорректно с новыми bundle ID/applicationId.
- `gs://rigel-psy.appspot.com/` в `firebase_cloud_storage.dart` (2 места) — реальный адрес Firebase Storage bucket, привязанный к текущему проекту. Менять эту строку в коде без соответствующей миграции/переименования бакета в Firebase — значит сломать все обращения к хранилищу (404). Трогать только синхронно с реальным изменением на стороне Firebase.
- `android/key.properties` (не в git, в `.gitignore`) — там же лежит путь `storeFile=key_rigel.jks`; сам файл ключа подписи и это локальное имя не переименовывались (не дизайн-ассет, а криптографический файл — переименование не запрашивалось и не требуется для работы).
- `android/.idea/workspace.xml` — локальный кэш IDE с абсолютными путями `/Users/roma/StudioProjects/Rigel_PSY/...` (машина предыдущего разработчика) — не трогали, это личное состояние IDE, не часть проекта, перегенерируется автоматически.
- **GitHub-репозиторий**: вы попросили переименовать `plotnikov228/Rigel_PSY` → `Riva_PSY`. В этом окружении нет `gh` CLI и нет доступа к аккаунту `plotnikov228` (судя по имени — не ваш аккаунт), поэтому переименование самого репозитория на GitHub я выполнить не могу. Если у вас есть права администратора на этот репозиторий — переименуйте через Settings → General → Repository name на github.com, или через `gh repo rename Riva_PSY` при наличии `gh` и прав. После переименования на GitHub git remote в локальных копиях обновится автоматически (GitHub держит редирект со старого URL), либо потребуется `git remote set-url origin <новый-URL>` вручную.

**Верификация:**
- `dart format --output=none` по всему `lib/` (366 файлов) — синтаксических ошибок нет ни в одном файле (просто показывает предсуществовавшие расхождения в форматировании, не связанные с переименованием).
- `flutter pub get` **не проходит** — но по причине, не связанной с переименованием: конфликт версий `awesome_notifications` и `easy_localization`/`intl`, зафиксированных в pubspec.yaml с 2024 года, против версии `intl`, которую тянет Flutter SDK 3.44.6, установленный в этом окружении. Это предсуществующая проблема совместимости зависимостей (нужно либо собирать более старым Flutter SDK, либо делать `flutter pub upgrade --major-versions` с проверкой всех пакетов) — она никак не зависит от строковых замен и была бы ровно такой же до переименования.
- Полный `flutter analyze`/сборку iOS или Android в этом окружении подтвердить не удалось (нет разрешённых зависимостей из-за пункта выше, и здесь нет Xcode/Android SDK toolchain для реальной сборки .ipa/.apk) — рекомендуется прогнать `flutter pub get` и сборку на машине с подходящей версией Flutter SDK перед публикацией.

## 8. Хроника исправления `flutter build apk --release` (2026-07-23)

Цель: получить полностью проходящую релизную сборку Android на этом окружении (Flutter 3.44.6, JDK 21/17 через SDKMAN, AGP 9.0.1, Gradle 9.1.0 — современный тулчейн, сильно новее, чем версии, под которые изначально писались многие зависимости в pubspec.yaml). Методология на всех 23 эпизодах была одинаковой: при любой новой ошибке — стоп, разведка (минимально достаточная версия/override, usage в коде, CHANGELOG на breaking changes, проверка конфликтов конкретно по архивам с pub.dev, а не по памяти), отчёт, и только после явного подтверждения — применение. Ни разу фикс не применялся без подтверждения.

**Статус на конец сессии: сборка НЕ проходит полностью.** Дошли до Firebase-барьера (см. эпизод 24 ниже) и сознательно остановились — апгрейд Firebase-семейства перенесён в отдельную сессию. `apksigner verify` не выполнялся — подписанного APK не существует.

### Инфраструктурные фиксы (не версии пакетов)

1. **Android Gradle Plugin: миграция на декларативный `plugins{}`.** `android/settings.gradle` переписан на `pluginManagement`/`plugins{}` (`dev.flutter.flutter-plugin-loader` 1.0.0, `com.android.application` 9.0.1, `org.jetbrains.kotlin.android` 2.3.20, `com.google.gms.google-services` 4.3.15). `android/app/build.gradle` получил `plugins{}` блок вместо старого `apply plugin`, добавлен `namespace "com.riva_psy.app"`, `lintOptions`→`lint`. `android/build.gradle` очищен от `buildscript{classpath}`, `jcenter()` убран. Причина: Flutter 3.44.6 требует новый Android v2 embedding и не работает со старым императивным стилем Gradle-конфигурации. Резервные копии `.bak-pre-migration` лежат рядом (удалить после финального подтверждения подписанной сборки — этот момент ещё не наступил, см. ниже).
2. **Gradle wrapper → 9.1.0.** `android/gradle/wrapper/gradle-wrapper.properties`: `distributionUrl` поднят до `gradle-9.1.0-all.zip` (был Gradle 7.x, несовместим с AGP 9).
3. **JDK 17 через `org.gradle.java.home` (не бамп пакета).** Проблема: `audio_session` падал с `-Werror` при компиляции под системный JDK 21. Перепробовано 4 варианта чисто через `android/build.gradle` (`gradle.projectsEvaluated`, `subprojects{afterEvaluate}`, `tasks.withType(JavaCompile)`, `suppressSourceTargetDeprecationWarning`) — ни один не сработал, все отменены. Финально сработал явный `org.gradle.java.home=~/.sdkman/candidates/java/17.0.13-tem` в `android/gradle.properties`, без единой правки кода/зависимостей. Инцидент по пути: `sdk install java 17.0.13-tem < /dev/null` из-за пустого stdin автоматически изменил системный JDK-дефолт (авто-"Y" на промпт) — поймано сразу через `sdk current java`, откачено обратно на `21.0.4-tem` через `sdk default java`, раскрыто пользователю.
4. **JVM target рассинхронизация Java/Kotlin.** После перехода на JDK 17 возникло `Inconsistent JVM Target Compatibility` (`compileReleaseJavaWithJavac`=11 vs `compileReleaseKotlin`=17). Фикс: явный `compileOptions{sourceCompatibility/targetCompatibility=VERSION_17}` + `kotlinOptions{jvmTarget="17"}` в `android/app/build.gradle`.
5. **`compileSdk` 34→36 + core library desugaring.** `flutter_local_notifications` и ряд плагинов требовали `compileSdk` ≥35/36 и/или desugaring. Фикс: `compileSdkVersion 36` (взято "с запасом", а не впритык 35, чтобы не возвращаться к вопросу при следующем плагине) + `coreLibraryDesugaringEnabled true` + `coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.5'` (сверено с maven-metadata.xml на dl.google.com — не гадали по памяти).
6. **`google-services.json` для нового package name.** После переименования `applicationId` → `com.riva_psy.app` (см. §7) старый `google-services.json` (project `rigel-psy`, package `com.rigel_psy.app`) вызывал `No matching client found for package name`. Пользователь сгенерировал новый файл в Firebase Console под новым package name (project `rigel-psy-9361c`) и поместил в `android/app/google-services.json` — проверено побайтово, что файл на месте и корректен, никакой путаницы с копиями в `~/Downloads`/архивных папках проекта не было.
7. **Новый release keystore.** `android/key_riva_psy.jks`, alias `riva_psy`. PKCS12 требует одинаковые store/key пароли (keytool предупредил при генерации) — `key.properties` (не в git) исправлен соответственно. Пароли нигде не выводились в чат/лог.

### Эпизоды апгрейда/override зависимостей (v1 Android embedding removal и смежные, по порядку)

Общий паттерн эпизодов 8–20: Flutter убрал поддержку "v1 Android embedding" — старые версии federated-плагинов падали с `cannot find symbol: class Registrar` (Java) или `Unresolved reference: shim/registerWith/ShimPluginRegistry` (Kotlin). Почти во всех случаях фикс — точечный `dependency_overrides` на `_android`-суб-пакет без бампа основного Dart-facing пакета.

8. **`awesome_notifications`** — изолирован и обновлён (первым, до начала releasee-build цепочки) как часть более ранней работы с `syncfusion_flutter_charts`/`easy_localization`/`intl` (см. §7 контекст).
9. **`syncfusion_flutter_charts`** → `^28.2.12` (прямой пакет), потребовал замены `ChartSeries<EmotionModel, String>` → `CartesianSeries<EmotionModel, String>` в [where_and_what_emotions_widget.dart](lib/presentation/charts/charts_screen/widgets/where_and_what_emotions_widget.dart) (API rename, не поведенческое изменение).
10. **`flutter_plugin_android_lifecycle`** override → `^2.0.20`.
11. **`file_picker`** → `^8.0.7` (прямой пакет, мажорный).
12. **`flutter_local_notifications`** → `^17.2.4` (прямой пакет, мажорный).
13. **`image_picker_android`** override → `^0.8.13`.
14. **`google_api_availability_android`** override → `^1.1.0`.
15. **`google_sign_in_android`** override → `^6.1.26`.
16. **Кластер `pdf`/`printing`/`flutter_svg`/`xml`/`archive`** — несколько раундов проб (printing 5.13.2+ требует `web ^1.0.0` → тянет `firebase_core ^3.4.1`, конфликт с пиненным `firebase_core ^2.27.2`); решено полным согласованным апгрейдом: `flutter_svg: ^2.3.0`, `pdf: ^3.13.0`, `printing: 5.13.1` (точная версия — **намеренно не выше**, см. эпизод 21 ниже), плюс `dependency_overrides`: `archive: ^4.0.9`, `pdf_widget_wrapper: ^1.0.4`, `win32: ^5.6.0`.
17. **Удаление `in_app_purchase`/`in_app_purchase_storekit`/`pay`** из [yookassa_payments.dart](lib/core/services/payments/yookassa_payments.dart) — не апгрейд, а удаление мёртвого/недостижимого кода: старая архитектура держала закомментированный IAP-путь и недостижимую (из-за хардкод-флага `_yookassa`) ветку Google/Apple Pay через `pay`. Оставлена только рабочая YooKassa (банковская карта) оплата. Полностью убраны: импорты обоих пакетов, `_yookassa` флаг, `_listenToPurchaseUpdated()`, `_servicePay()`, `ExamplePaymentQueueDelegate`.
18. **`path_provider_android`** override → `^2.2.5`.
19. **`permission_handler_android`** override → `^12.0.7`, потребовал мажорного бампа прямого `permission_handler: ^10.4.5 → ^11.3.0` (structural-конфликт: `permission_handler_platform_interface` версии не пересекались без бампа основного пакета).
20. **`shared_preferences_android`** override → `^2.2.3`.
21. **`sqflite`** override → `^2.3.3+1`.
22. **`sign_in_with_apple`** мажорный бамп `^5.0.0 → ^6.1.1` — проверено, что `getAppleIDCredential`/`AppleIDAuthorizationScopes`/`WebAuthenticationOptions`/`.isAvailable()` в [services_auth_service.dart](lib/presentation/initial_setup/sign_in/services/services_auth_service.dart) компилируются без изменений API.
23. **`url_launcher_android`** override → `^6.3.3`.
24. **`workmanager`** мажорный бамп `^0.5.1 → ^0.6.0`. Важная находка: реальный вызов `Workmanager().initialize()`/`.registerPeriodicTask()` в [workmanager_service.dart](lib/core/services/workmanager/workmanager_service.dart) **закомментирован** — пакет фактически мёртв на рантайме, все напоминания (включая Pills) идут через `AwesomeNotificationService` (пакет `awesome_notifications`), не через настоящий Android WorkManager API.
25. **`audio_session`** override → `^0.1.22`.
26. **`just_audio`** мажорный бамп `^0.9.36 → ^0.9.40` — проверено, что используемый API (`AudioPlayer`, `setUrl`/`setAudioSource`/`play`/`pause`/`seek`, без `ConcatenatingAudioSource`/`LoopingAudioSource`) не деприкейтится вплоть до `0.10.0` (которую не берём).
27. **`record_android` + `record_platform_interface` — двойная точная фиксация.** Это единственный эпизод с реальными ошибками/откатами внутри разведки:
    - Первая попытка `record_android: ^1.2.6` (каретка) резолвнулась в `1.5.2`, что подняло `record_platform_interface` транзитивно до `1.6.0` и сломало `record_linux 0.7.1` (`Unresolved reference: startStream`/несовпадающая сигнатура `hasPermission`) — платформенный Linux-пакет, физически компилируемый в общий kernel snapshot даже при таргете Android.
    - Исправлено точной фиксацией `record_android: 1.2.6` (без каретки) — но это **не ограничило** `record_platform_interface` (пин версии пакета не капает версии его собственных транзитивных зависимостей), тот остался на `1.6.0`.
    - Добавлен override `record_platform_interface: 1.3.0` (точно) — ошибочная первая попытка: неверно интерпретировал грep-фрагмент CHANGELOG/исходников и решил, что `startStream` остаётся конкретным (не абстрактным) методом вплоть до `1.3.0`. Билд опроверг это.
    - Прочитан полный исходный код метода по каждой версии: `startStream` имеет тело по умолчанию (`=> throw UnimplementedError(...)`) в `1.0.2`–`1.2.0`, становится абстрактным именно в **`1.3.0`** (тело убрано). Граница `1.2.0`/`1.3.0`, не `1.3.0`/`1.4.0`, как думали раньше.
    - Финальная фиксация: `record_platform_interface: 1.2.0` (точно). `record_android` не имеет собственной `lib/`-директории (чистый Android-нативный пакет, метод-channel), поэтому версия platform_interface для его Kotlin-кода значения не имеет.
    - Ложный повторный провал: после этого фикса всплыла ошибка `Unresolved reference: PermissionManager/AudioFormats/...` **внутри самого** `record_android 1.2.6` — десятки "unresolved" по файлам одного и того же модуля, при том что все файлы физически на месте на диске. Диагностировано как повреждённый инкрементальный Gradle/Kotlin-кэш (возможно, отголосок более раннего прерывания сборки приостановкой ноутбука). Подтверждено: `flutter clean` + `cd android && ./gradlew clean` + пересборка с нуля полностью устранили ошибку без единой правки кода.

### Осознанно отложено (не эпизоды-фиксы — открытые решения)

28. **`printing` (compileSdk 30) — НЕ трогать до Firebase-апгрейда.** Проверено бинарным поиском: уже самая первая следующая версия (`5.13.2`) требует `web: ^1.0.0`, а все текущие Firebase-web-пакеты (`firebase_core_web 2.13.0`, `firebase_auth_web 5.10.1`, `firebase_storage_web 3.8.1`, `firebase_messaging_web 3.7.1`) жёстко требуют `web: ^0.5.1`. Это структурный конфликт, не версионный зазор — безопасной промежуточной версии `printing` не существует. Апгрейд возможен только вместе с Firebase-семейством.
29. **Firebase-семейство (6 пакетов) — следующая большая задача, отдельная сессия.** `cloud_firestore 4.15.10`, `firebase_analytics 10.9.0`, `firebase_auth 4.18.0`, `firebase_core 2.27.2`, `firebase_messaging 14.7.21`, `firebase_storage 11.6.11` — все со старым `compileSdk 33` (нужно ≥34). Это граница, на которой сборка сейчас **фактически падает** (`:cloud_firestore is currently compiled against android-33` — 15 issues в `checkReleaseAarMetadata`, тот же паттерн, что был у `just_audio`/`audio_session`/`record_android`, но здесь фикс структурно требует полного скоординированного апгрейда всей связки, а не точечного override). Именно из-за этой связки `printing` (эпизод 28) тоже пока не трогаем — апгрейды придётся делать вместе.

### 30. Firebase-апгрейд — барьер снят (2026-07-23, отдельная сессия)

Апгрейд всей связки разом, одним согласованным набором версий (методология — та же: минимально достаточная версия с `compileSdk` ≥ 34, проверено по реально скачанным архивам, не по памяти):

```
firebase_core:      2.27.2  → 2.28.0
cloud_firestore:    4.15.10 → 4.16.0
firebase_auth:      4.18.0  → 4.19.0
firebase_messaging: 14.7.21 → 14.8.0
firebase_storage:   11.6.11 → 11.7.0
firebase_analytics: 10.9.0  → удалён из pubspec.yaml полностью
```

Ключевая находка: FlutterFire выпускает **синхронизированные релизы** — один и тот же PR ([#12566](https://github.com/firebase/flutterfire/issues/12566), "Bump compileSdk version of Android plugins to latest stable (34)") вышел одновременно во всех 6 пакетах, каждый раз минорным шагом +1 от версии, которая была у нас. Ни одной **BREAKING**-записи в CHANGELOG между текущими и целевыми версиями ни у одного из пяти апгрейженных пакетов. Проверка конфликта с `web` (та же история, что была с `printing`/`pdf`/`flutter_svg`) — **не подтвердилась**: все Firebase-web-сиблинги на этих минимальных версиях всё ещё требуют `web: ^0.5.1`, бамп до `web: ^1.0.0` происходит у Firebase позже, не на этом шаге.

`firebase_analytics` — подтверждено `grep`, что нигде не используется в `lib/` (ни прямых вызовов, ни `NavigatorObserver`, ни инициализации в `main.dart`) — удалён из `pubspec.yaml` целиком, а не апгрейднут.

**Результат:** `flutter build apk --release` **прошёл всю Firebase-связку впервые за всю миграцию** (5м12с чистого прогресса, ни одной ошибки, связанной с Firebase/compileSdk 33). Firebase-барьер как таковой закрыт.

Сборка упала на **новом, не связанном с Firebase блокере** — том же паттерне, что раньше был у `just_audio`/`audio_session`/`record_android` (модуль с собственным захардкоженным `compileSdk`, теперь ниже требования от обновлённой транзитивной зависимости):

```
Execution failed for task ':file_picker:checkReleaseAarMetadata'.
> Dependency ':flutter_plugin_android_lifecycle' requires compileSdk ≥ 36.
  ':file_picker' is currently compiled against android-34.
```

Разведка по `file_picker` — следующий эпизод (31).

**Текущее состояние на конец сессии:**
- Два коммита на ветке `pre-pwa-migration-snapshot`, запушены в `origin` (GalaRiva/RIVA_PSY):
  1. `Fix release build: dependency overrides, JDK 17, major package upgrades, remove dead in_app_purchase code, new release keystore, updated google-services.json for renamed package`
  2. `Add English/Spanish localization, fix duplicate key, fix locale country code mismatch`
  3. (этот) `Firebase upgrade: bump 5 packages to compileSdk-34 minimums, remove unused firebase_analytics`
- `flutter pub get` и `flutter analyze` — 0 ошибок.

### 31. `file_picker` vs `flutter_plugin_android_lifecycle` — разведка и фикс (2026-07-23)

`file_picker` апгрейд оказался заблокирован тем же структурным конфликтом, что и `printing`: начиная с версии `8.1.0` пакет требует `web: ^1.0.0`/`^1.1.0`, что конфликтует с Firebase-web-семейством (`web: ^0.5.1`). Проверено «вариант 1»: даже после Firebase-апгрейда (эпизод 30) реально резолвнутые Firebase-web-пакеты (`firebase_core_web 2.17.1`, `firebase_auth_web 5.12.0`, `firebase_messaging_web 3.8.7`, `firebase_storage_web 3.9.7`) всё ещё требуют `web: ^0.5.1` — не сдвинулось. Значит `file_picker` **тоже остаётся** на `^8.0.7` (подтверждено: нигде не используется в `lib/`, `pub get` резолвится чисто).

Реальный фикс лежал не в `file_picker`, а в override `flutter_plugin_android_lifecycle`: открытый `^2.0.20` резолвился в самую свежую (`2.0.35`), которая с версии `2.0.25` перестала хардкодить `compileSdk` и стала наследовать его динамически из `flutter.compileSdkVersion` (=36 у нас) — из-за этого требовала от соседей (`file_picker`, всё ещё на compileSdk 34) тоже быть на 36. Нашёл окно `2.0.20–2.0.24`, где фикс v1-embedding уже есть, а `compileSdk` ещё хардкожен на `34` (совместимо с `file_picker`). Зафиксировано точно: **`flutter_plugin_android_lifecycle: 2.0.24`** (без каретки).

Результат: конфликт `file_picker`/`flutter_plugin_android_lifecycle` полностью устранён — сборка прошла этот этап впервые. `flutter build apk --release` продвинулась дальше и упёрлась в **последний оставшийся блокер — сам `printing`** (compileSdk 30, эпизод 28), который структурно привязан к тому же самому конфликту с `web`/Firebase-web, что и был описан выше. Firebase-web не сдвинулся с `web ^0.5.1` даже после апгрейда самого Firebase (проверено только что), так что деблокировка `printing` по-прежнему невозможна без отдельного решения (замена пакета, либо override только Android-части без общего web-констрейнта, либо ожидание, когда Firebase-web сам перейдёт на `web ^1.x`).

### 32. `printing` — разведка замены на `syncfusion_flutter_pdfviewer`, тупик, и корневое решение (2026-07-23)

**Разведка federated-архитектуры `printing`:** проверено напрямую в `pubspec.yaml` последней версии (`5.15.0`) — `printing` монолитный (endorsed) плагин, `printing_android` как отдельного пакета не существует. Путь через точечный override Android-части (как с `record_android`) недоступен.

**Глубокая разведка использования** (по факту, чтением каждого места вызова, не по названиям методов):
- [pdf_viewer_widget.dart](lib/widgets/pdf_viewer_widget.dart) — `PdfPreview(build: pdf, canChangeOrientation: false, canChangePageFormat: false)`. Источник байт — не файл/network, а callback `FutureOr<Uint8List> Function(PdfPageFormat)`. `allowPrinting`/`allowSharing` не переопределены — оба `true` по умолчанию (проверено в исходниках `pdf_preview.dart`), обе кнопки в toolbar активны.
- [report_model.dart](lib/presentation/charts/charts_screen/models/report_model.dart) и [alternative_pdf.dart](lib/presentation/recomendation/recomendation_screen/working_out/working_out_irrational/pages/alternative/alternative_pdf.dart) — вызовов `Printing.*` НЕТ вообще; единственное использование пакета — `PdfGoogleFonts.rubikRegular()`/`rubikBold()` (шрифты, физически живут в `printing/lib/src/fonts/gfonts.dart`, не в `pdf`).
- `path_final_screen/controller.dart` — **исправлена ошибка более раннего отчёта в этой же сессии**: файл вообще не использует `printing`, был ошибочно включён в список раньше.
- Граница "генерация через `pdf:`, показ/печать через `printing`" подтвердилась **частично**: генерация PDF (`Document`/`Page`/`pdf.save()`) — целиком `pdf:`, но `printing` также используется на этапе генерации как источник шрифтов (`PdfGoogleFonts`), не только для показа.

**Попытка замены (Часть 1 — шрифты, применена):** скачаны статичные `.ttf` Rubik Regular/Bold напрямую с `fonts.gstatic.com` (Open Font License) → `assets/fonts/RubikRegular.ttf`, `assets/fonts/RubikBold.ttf`. `PdfGoogleFonts.rubikRegular()/rubikBold()` заменены на `Font.ttf(await rootBundle.load('assets/fonts/Rubik{Regular,Bold}.ttf'))` (API сверен в исходниках `pdf-3.13.0/lib/src/widgets/font.dart`) в обоих генераторах. `import 'package:printing/printing.dart'` убран из обоих файлов — оба больше не используют пакет `printing`.

**Попытка замены (Часть 2 — viewer, ЗАБЛОКИРОВАНА, не применена):** при проверке версии `syncfusion_flutter_pdfviewer`, совместимой с зафиксированным `syncfusion_flutter_charts: ^28.2.12` — обнаружено, что **вся линейка `28.x`** (`28.1.33`…`28.2.12`, без исключений) требует `web: ^1.0.0` напрямую. `share_plus` (latest `13.2.1`) и `open_file_web` — та же стена, `web: ^1.1.1`/`^1.1.0`. План замены упёрся бы в точно тот же конфликт, от которого пытались уйти.

**Разведка корня, а не симптома:** найдена точка перехода Firebase-web-семейства на `web ^1.x` бинарным поиском по каждому из 5 пакетов (не по `latest`, а именно по первой версии, где транзиция происходит):
```
firebase_core:      2.32.0 → 3.5.0   (firebase_core_web впервые web^1.x на 2.18.0)
cloud_firestore:    4.17.5 → 5.5.0
firebase_auth:      4.20.0 → 5.3.0
firebase_messaging: 14.9.4 → 15.2.0
firebase_storage:   11.7.7 → 12.4.0
```
Ровно 1 мажорная версия у каждого пакета. BREAKING-записи именно в этом диапазоне (не по всей истории пакета) — только синхронизированные инфраструктурные строки (`minSdk 21/23`, iOS deployment target 13 — оба требования уже выполнены нашим текущим конфигом) плюс по одной строке "remove deprecated API" у `firebase_auth`/`firebase_storage`. Расшифрованы через GitHub PR (`gh` CLI недоступен в этом окружении, использован WebFetch):
- `firebase_auth 5.0.0` (PR #12859): удалены `useEmulator()`, `signInWithAuthProvider()`, `User.updateProfile()`.
- `firebase_storage 12.0.0` (PR #12863): удалён `useEmulator()`.

Сверено по всему `lib/` (`grep`, не только Auth/Storage-группы) — **пересечений с нашим кодом нет ни одного**.

**Применено:** апгрейд всех 5 Firebase-пакетов до версий перехода (резолвнулось выше: `firebase_core 3.15.2`, `cloud_firestore 5.6.12`, `firebase_auth 5.7.0`, `firebase_messaging 15.2.10`, `firebase_storage 12.4.10`), следом `printing: 5.13.1 → ^5.15.0` (потолок снят — причина его существования, конфликт с Firebase-web, только что устранена). `flutter pub get` резолвил всё разом на единой `web: 1.1.1` — Firebase-web-сиблинги и `printing` больше не конфликтуют. Проверено: `printing 5.15.0` — `compileSdk 34` (в реально скачанном архиве) — сам `printing` больше не блокер вообще, план замены на `syncfusion_flutter_pdfviewer` стал ненужным.

### 33. ФИНАЛ МИГРАЦИИ — первый реально собранный и подписанный релизный APK (2026-07-23)

`flutter build apk --release` **прошёл полностью, впервые за всю миграцию**: `✓ Built build/app/outputs/flutter-apk/app-release.apk (95.5MB)`.

`apksigner verify --print-certs`:
```
Signer #1 certificate DN: CN=RIVA PSY, OU=RIVA PSY, O=RIVA PSY, L=Kemerovo, ST=Kemerovo, C=RU
Signer #1 certificate SHA-256 digest: 43fd3f8ae581c06577a81c9fbb9af15e9ab8508107568f4e65d7b93389bafacb
```
Подпись подтверждена, ключ `android/key_riva_psy.jks` (alias `riva_psy`) работает корректно.

**Все блокеры за всю миграцию устранены.** Полный список: 23 эпизода первичной цепочки build-fix (Gradle-миграция, JDK 17, 14 dependency_overrides, мажорные апгрейды permission_handler/sign_in_with_apple/workmanager/just_audio/audio_session, удаление in_app_purchase, новый keystore) + Firebase-семейство (сначала compileSdk-фикс минорными версиями, затем полный переход на `web ^1.x` мажорными версиями) + `file_picker`/`flutter_plugin_android_lifecycle` + `printing` (сначала обходили конфликт с web, в итоге он снялся сам после Firebase-апгрейда) + локализация EN/ES.

### 34. Release-краш на реальных устройствах — отсутствующие R8/ProGuard keep-правила (2026-07-24)

Собранный в эпизоде 33 APK успешно ставился и подписывался, но **крашился при запуске на реальных устройствах** (debug не пробовали — сразу release):
```
java.lang.RuntimeException: Unable to get provider androidx.startup.InitializationProvider
Caused by: java.lang.NoSuchMethodException: androidx.work.impl.WorkDatabase_Impl.<init> []
```

**Диагностика (до применения фикса):** дерево зависимостей `androidx.work` резолвится в единую версию `2.10.4` без конфликта (`workmanager: ^0.6.0` просит `2.9.0`, `app` просит `work-runtime-ktx:2.8.1`, `yookassa_payments_flutter` тянет `2.10.4` — Gradle берёт максимум для всех). Реальная причина — системный пробел: `workmanager 0.6.0` не поставляет ни `consumer-rules.pro`, ни каких-либо ProGuard-файлов вообще, а `android/app/build.gradle` (проверено через `git log` — было так и **до** всех сегодняшних миграций, не регрессия) никогда не объявлял `proguardFiles` при `minifyEnabled true`. Разбор `mapping.txt` подтвердил: класс `WorkDatabase_Impl` сохранён (имя не переименовано), но его конструктор без аргументов вырезан R8 — Room создаёт `_Impl`-классы способом, который R8 без явного keep-правила не распознаёт как точку входа. Крашится через `androidx.startup.InitializationProvider` — WorkManager инициализируется автоматически при старте процесса на Android-уровне, независимо от того, что вызов `Workmanager().initialize()` в Dart-коде давно закомментирован (эпизод 24).

**Фикс, часть 1 — `android/app/proguard-rules.pro` создан:**
```proguard
-keep class androidx.work.impl.** { *; }
-keep class * extends androidx.work.Worker
-keep class * extends androidx.work.InputMerger
-keep public class * extends androidx.work.ListenableWorker
```
`android/app/build.gradle`, `release`-блок: добавлена строка `proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'`.

После пересборки `WorkDatabase_Impl` краш исчез (проверено установкой на эмулятор + `flutter run`/скриншот — приложение открывается на экране регистрации). Но в логе всплыла **та же корневая причина** для другого механизма: `ComponentDiscovery` (Firebase) не мог инстанцировать через рефлексию 5 классов `*Registrar` (`FirebaseAnalyticsLegacyRegistrar`, `FirebaseAppCheckKtxRegistrar`, `FirebaseAppCheckRegistrar`, `FirebaseInstallationsKtxRegistrar`, `FirebaseMessagingKtxRegistrar`) — конструкторы тоже вырезаны, что подтвердил `mapping.txt`. В отличие от WorkManager, Firebase's `ComponentDiscovery` оборачивает каждую попытку в try-catch — приложение **не падает**, просто теряет эти опциональные компоненты.

**Фикс, часть 2 — правило добавлено, но с первой попытки НЕ сработало.** Официальное Firebase-правило `-keep public class * implements com.google.firebase.components.ComponentRegistrar` (без блока `{ ... }`) защитило только сами классы от удаления/переименования (подтверждено в `seeds.txt`), но не защитило их члены — конструктор всё ещё вырезался (стандартная семантика ProGuard/R8: `-keep class X` без явного блока не форсирует сохранение членов, не вызываемых из видимого графа статических вызовов). Уточнено до:
```proguard
-keep public class * implements com.google.firebase.components.ComponentRegistrar {
    public <init>();
}
```
После этого конструкторы у всех пяти классов подтверждены в `mapping.txt` (`void <init>():X:X -> <init>`), `ComponentDiscovery`-предупреждения полностью исчезли из логов (было 5 записей `NoSuchMethodException`, стало 0).

**Отдельно всплывшая, не связанная с ProGuard проблема:** в логе оставалась `FirebaseMessaging: Firebase Installations Service is unavailable / SERVICE_NOT_AVAILABLE`. Доказано A/B-сравнением (ошибка идентична и до, и после полного исчезновения `ComponentDiscovery`-предупреждений), что она **не связана с ProGuard** — вероятная причина: на использованном эмуляторе не был добавлен Google-аккаунт (`dumpsys account` → `Accounts: 0`). Добавление аккаунта не выполнялось намеренно — ввод учётных данных в форму входа Google является действием, которое не выполняется автономно ни при каких условиях. Требует проверки на реальном устройстве с уже настроенным Google-аккаунтом.

**Текущее состояние на конец сессии:**
- Релизная сборка **проходит полностью, подписывается корректно, устанавливается и запускается без крашей** (проверено на эмуляторе; ожидает подтверждения на реальных устройствах пользователем).
- `SERVICE_NOT_AVAILABLE` у `FirebaseMessaging` — открытый вопрос, не блокирующий, требует теста на реальном телефоне с Google-аккаунтом, не относится к коду/сборке.
- **Важный урок на будущее:** до сегодняшнего дня минификация R8 (`minifyEnabled true`) работала в проекте вообще без единого явного keep-правила — потому что это первая когда-либо запущенная и успешно собранная release-сборка за всю историю проекта. Подобные пропуски могут всплывать и у других библиотек по мере дальнейшего реального тестирования (не только WorkManager/Firebase-ComponentDiscovery) — стоит держать это в уме при следующих найденных рантайм-проблемах на release-сборке.
- `.bak-pre-migration` файлы — удалены (эпизод завершён отдельно, до этого краша).
- Следующий разумный шаг вне этой сессии — тестирование на реальных устройствах (подтвердить исчезновение краша + разобраться с `SERVICE_NOT_AVAILABLE` при наличии Google-аккаунта), затем возврат к PWA-миграции (основная цель ветки `pre-pwa-migration-snapshot`).

### 35. `flutter run` в фоне через именованный пайп (FIFO) — находка про однократный `cat`-reader (2026-07-25)

Во время сессии поэкранной локализации UI (коммит `5fd946c` на `pre-pwa-migration-snapshot`, 2026-07-25) использовался `flutter run -d <device> --debug` в фоне с интерактивной подачей команд `r`/`R` (hot reload/restart) через именованный пайп из отдельных вызовов инструмента, например:

```bash
mkfifo /tmp/flutter_run_input
(cat /tmp/flutter_run_input; sleep 999999) | flutter run -d emulator-5554 --debug > /tmp/flutter_run_session.log 2>&1 &
# в другом вызове, позже:
printf 'R' > /tmp/flutter_run_input
```

**Проблема:** такая схема работает только один раз. `cat` читает из FIFO до EOF, который наступает сразу после того, как писатель (`printf ... > fifo`) закрывает файл — то есть после самой первой команды `cat` завершается и выходит. Обёртка `(cat fifo; sleep 999999)` не спасает: `sleep 999999` ничего не делает с пайпом, он просто держит процесс живым, но уже не читает. В результате первая отправленная команда (`R`) доходит до `flutter run` и честно перезапускает приложение, а все последующие `printf 'R' > /tmp/flutter_run_input` **блокируются навечно** в ожидании открытия FIFO на чтение — реального читателя больше нет. Симптом со стороны: вызов инструмента с записью в пайп зависает и уходит по таймауту в фон, а в логе `flutter run` не появляется новая строка `Performing hot restart...`/`Restarted application in ...` — визуально выглядит так, будто изменения в коде не подхватились, хотя на самом деле команда перезапуска просто не была доставлена.

**Фикс — заменить `cat` на `tail -f`:**

```bash
mkfifo /tmp/flutter_run_input
tail -f /tmp/flutter_run_input | flutter run -d emulator-5554 --debug > /tmp/flutter_run_session.log 2>&1 &
```

`tail -f` не завершается на EOF — он продолжает ждать новые данные в файле вечно, поэтому одного и того же процесса-читателя хватает на весь сеанс `flutter run`, сколько бы раз ни отправлялись `printf 'r'`/`printf 'R'` в файл пайпа. Побочный нюанс, на который стоит обратить внимание при повторении: если случайно запустить `tail -f fifo` дважды (например, тестовым запуском в фоне "для проверки", не убив его перед основным запуском) — оба процесса будут читателями одного FIFO одновременно, и записанные байты непредсказуемо достанутся то одному, то другому, из-за чего часть команд `r`/`R` будет "теряться" без явной ошибки. Перед стартом сессии стоит проверять `ps aux | grep "tail -f <путь-к-fifo>"` и держать ровно один читатель.

### 36. Локализация экрана "Графики" (charts_screen/charts_calendar) — коммит `c8a179d`, 2026-07-25

Переведены `k61_screen.dart` (6 табов + заголовок), `report_widget.dart`, `what_emotion_widget.dart`, `where_and_what_emotions_widget.dart`, `where_in_body_widget.dart`, `charts_calendar/k62_screen.dart`, embedded Pills-виджеты `admission_schedule/*` (используются как один из табов экрана "Графики"), и `report_model.dart` (текст, зашитый в генерируемый PDF-отчёт). Почти все строки совпали точно с уже существующими ключами в `assets/translations/*.json` — похоже, этот блок ключей (`charts`, `state_diagnosis`, `summary_report`, `date`/`situation`/`emotions`/`body`/`actions`/`thoughts` и т.д.) был подготовлен заранее, но не был подключён через `.tr()` до этой сессии.

Отдельная находка: `where_in_body_widget.dart` рисует фразу "Где в теле живут [позитивные/негативные] эмоции" тремя раздельными `Text`-виджетами, чтобы выделить слово "позитивные"/"негативные" жирным. Порядок слов вокруг этого слова разный по языкам (RU/EN — прилагательное перед "эмоции"/"emotions", ES — после "emociones"). Решено новой парой шаблонных ключей `where_body_emotions_prefix`/`where_body_emotions_suffix` (без общей позиции `{}` — просто prefix+word+suffix одинаковым порядком склейки для всех трёх языков, т.к. сам текст prefix/suffix уже учитывает разный порядок слов), а само слово берётся из уже существующих ключей `positive`/`negative` (уже в нужной грамматической форме, включая согласование по роду во множественном числе в испанском — "positivas"/"negativas") через `.toLowerCase()`. Проверено сборкой на скрипте, что результат совпадает с уже существующими цельнофразовыми ключами `where_positive/negative_emotions_reside_in_the_body` — подтверждено визуально на эмуляторе (жирное слово в нужном месте, во всех языках).

**Не проверено визуально, перенесено по точному совпадению ключей (риск минимальный, но не подтверждено на экране):**
- `admission_schedule/widgets/schedule.dart` — тексты `if_taken_slightly_later_no_problem`, `show_x_days`, `show_month`, `change_medication_intake_schedule` рендерятся только после того, как у пользователя уже создана реальная схема приёма препарата (`scheduleWasCreated == true`), что требует прохождения формы добавления препарата в Pills-модуле — не проверялось в этой сессии.
- `report_model.dart` — текст внутри самого PDF-файла (`mobile_application`, `convenient_and_free_record_and_send_SMER_to_psychologist`, `SMER`, шапка таблицы через `_columnTags`) — требует открыть PDF-превью (кнопка "Send summary report..." на экране), не проверялось.

Стоит свериться при следующем удобном случае — например, когда будем тестировать Pills-модуль целиком или PDF-экспорт, не отдельным заходом ради одной этой проверки.

**Инцидент с эмулятором в этой же сессии:** `flutter run` дважды подряд зависал на "Performing hot restart..."/"Installing..." без завершения (не крашился, просто не прогрессировал), при этом `adb`/`boot_completed` отвечали нормально — не совпадало с ранее описанным паттерном "system service crashed". Простой перезапуск процесса `flutter run` не помог (завис снова на install). Полный cold boot эмулятора (`emulator -avd Pixel_7a_API_35 -no-snapshot-load`) решил проблему с зависанием `flutter run`, но сразу после cold boot сам эмулятор кратковременно давал ANR ("isn't responding") на только что запущенное приложение и `/sdcard` был временно недоступен для `adb shell`/`screencap` (`Transport endpoint is not connected`) — обходной путь: писать скриншоты через `adb shell screencap -p /data/local/tmp/...` и `adb pull` оттуда вместо `/sdcard/...`, сработало сразу. На момент записи этого пункта оба симптома (ANR, недоступный `/sdcard`) казались временными — **уточнение см. §37**: ANR на этом же cold-boot инстансе оказался не разовым, а устойчиво повторяющимся при любом экране, который делает сетевой запрос к Firestore.

### 37. Локализация экрана "Рекомендации и упражнения" (recomendation_screen, верхний уровень) — коммит `4f61ec2`, 2026-07-25

Переведены `k70_screen.dart` (заголовок, хэдинг, кнопка "Помощь при панике", 2 верхних таба), `widgets/exercises_tab_body.dart` (4 таба: Введение/Медитации/Негативные эмоции/Депрессия), `models/tabs/negative_emotions_model.dart` (список из 11 табов групп негативных эмоций + общий текст-инструкция про дыхание), `models/tabs/depression_model.dart` (тот же текст-инструкция), `core/services/negative_emotion_tabs.dart` (сообщение об ошибке загрузки табов). В объём **не включался** `working_out/` (это отдельный экран "Обретение" по структуре Figma, см. §2) — только верхний уровень `recomendation_screen`.

**Важная находка про сам экран**: большая часть видимого контента (названия/аудио карточек рекомендаций, реальные названия 11 групп негативных эмоций) приходит из Firestore (коллекции `Audio`, `Tabs`, `Text_Recommendation`), а не из хардкода в Dart — поэтому изначальная оценка "~175 строк" не подтвердилась (реально страниц с литералами — 23 строки). Хардкодный список из 11 Tab-виджетов в `negative_emotions_model.dart` — по факту почти мёртвый код (немедленно перезаписывается результатом из Firestore в `getTabBodies()`), но переведён по решению пользователя, т.к. теоретически может на долю секунды промелькнуть при холодном старте до того как Firestore-данные подгрузятся (сам `TabBar` в `exercises_tab_body.dart` не обёрнут в тот же `GetBuilder`, что и содержимое табов).

**Сознательно не тронуто** (data-matching, не UI-текст): `audio.name == 'Введение'` в `depression_model.dart`/`introduction_model.dart` (сравнение с полем, пришедшим из Firestore), `russianAlphabet`-константа для сортировки в `negative_emotions_model.dart`, `CurrentUser.user.currentTariff!.name == 'Базовый'` в `tab_widget.dart` (решает показ пейвола).

**Найден и исправлен попутный баг**: ключ `introduction` существовал только в `ru-RU.json` (строка 707), отсутствовал в `en-US.json`/`es-ES.json` целиком. На английской/испанской локали таб "Введение" показывал буквально `introduction` (сырое имя ключа, строчными буквами) вместо переведённого текста — характерный симптом отсутствующего в конкретном языковом файле ключа при рабочем fallback-механизме `easy_localization` (тихо возвращает исходную строку). Добавлены `"introduction": "Introduction"` / `"Introducción"` в оба файла. Стоит держать в уме: раз такое случилось с одним ключом, не исключено, что есть и другие ключи, присутствующие не во всех трёх файлах — целенаправленная сверка всех трёх JSON на предмет расхождения набора ключей (не только значений) могла бы быть отдельной точечной задачей.

**Не проверено визуально из-за окружения**: `negative_emotions_model.dart` (11 табов), `depression_model.dart` (`breathing_relaxation_instruction`), `negative_emotion_tabs.dart` (`unexpected_error_check_connection`) — экран завис на загрузке из Firestore (спиннер не разрешился за 15+ секунд ожидания, ANR "isn't responding" всплыл 4 раза подряд на одном и том же cold-boot инстансе эмулятора из §36). Все переведены по точному совпадению уже существующих ключей (`wrath`/`panic`/`fear2`/`sorrow`/`resentment`/`uncertainty`/`disgust`/`guilt`/`laziness`/`loneliness`/`lostness` и новые `breathing_relaxation_instruction`/`unexpected_error_check_connection`), риск минимальный, но не подтверждено на экране. **Уточнение к §36**: тогдашняя формулировка "ANR был временным" не подтвердилась — тот же cold-boot инстанс эмулятора снова дал повторяющийся ANR в этой сессии на другом экране с похожим паттерном (сетевой запрос при открытии экрана). Похоже на устойчивую деградацию сетевого стека/доступа к Firestore именно на этом инстансе после cold boot, а не на случайную единичную просадку. Сверить оставшиеся 3 файла при следующей возможности — на свежем эмуляторе или на реальном устройстве.

### 38. Path-экраны ("Что произошло?" и далее) — архитектурная разведка + локализация UI-текста, коммит `5ae89fb`, 2026-07-26

**Архитектурная находка (до применения правок, по прямому запросу пользователя — "не рискуя сломать данные существующих пользователей").** Все 5 непустых `repository.dart` в `lib/presentation/main/path/` (`what_emotion_screen`, `what_body_parts_screen`, `what_happened_screen`, `where_happened_screen`, `with_who_happened_screen`) устроены одинаково: `EventModel`/`BodyPartsModel` хранят русский текст (`name`/`bodyPart`) как **единственный** идентификатор, без отдельного стабильного ключа. При первом чтении пустого Hive-бокса захардкоженный русский seed-список записывается в Hive как есть; выбор пользователя на экране прикрепляется к `DayEventModel` и сохраняется вместе с диари-записью — русская строка становится частью пользовательских данных навсегда, не просто отображаемым текстом. Эти же значения потом читаются: уже локализованным экраном "Графики" (группировка/подсчёт по `.name`, парсинг `+`/`-` суффикса), `record_add/edit_screen`'ами и `what_body_parts_screen/controller.dart` (`switch(model.bodyPart.toLowerCase())` для расчёта высоты диалога/роста), и самими picker-контроллерами (поиск по подстроке, сравнение "уже выбрано"). Итого 284 из 401 строки кириллицы в этом дереве — данные, не текст интерфейса; **не тронуты в этом заходе**.

**Локализован только чистый UI-текст (117 строк вне `repository.dart`)** — заголовки, хинты, кнопки, snackbar-сообщения на всех 8 экранов пути (`what_happened`, `where_happened`, `with_who_happened`, `what_emotion`, `additional_emotions`, `what_body_parts`, `what_i_do`, `first_thoughts`, `path_final` + виджеты). Каждый контроллер/файл проверен построчно на скрытые сравнения по образцу уже известного `what_body_parts_screen/controller.dart` (`case 'голова и лицо':` и т.п., не тронут). В процессе найдено **ещё одно** такое место, не обнаруженное при первой разведке: `what_emotion_screen/k27_screen.dart` (строки с `'Негативные'`/`'Позитивные'`/`'Нейтральные'`) и одноимённый fallback в `additional_emotions_screen/k31_screen.dart:25` — эти строки round-trip'ятся через `string_extension.dart`'s `getEmotionTypeFromString()` (switch по точным русским строкам) обратно в enum `EmotionInDayEvent`, который потом пишется в `DayEventModel.emotionInDayEvent`. Перевод сломал бы классификацию эмоций на все языки кроме русского — оставлено нетронутым.

Исправлены попутно два копипаст-бага (оба места "Событие не найдено" на самом деле принадлежали экранам про место/персону — заведены отдельные `place_not_found_add_your_own`/`person_not_found_add_your_own`) и лишний апостроф в кнопке `"выбор эмоции'"`.

**Два TODO-маркера оставлены без ключа, ждут решения пользователя** (не изобретены самовольно):
- `what_body_parts_screen/k32_screen.dart:331` — `'Выберете часть тела или создайте новую'`.
- `where_happened_screen/k25_screen.dart:291` — `"выбор события"` (этот экран — про МЕСТО, не событие; похоже на тот же класс копипаст-бага, что уже поправили для "не найдено", но здесь решение не принято).

**Key/value-рефакторинг для 5 сущностей (эмоции/события/места/люди/части тела) — отдельная, ещё не начатая задача.** Обсуждали архитектуру: добавить в `EventModel`/`BodyPartsModel` стабильное языконезависимое поле-ключ, переключить все точки сравнения (5 picker-контроллеров + `charts/controller.dart` + оба height-switch) на сравнение по ключу, `.name`/`.bodyPart` оставить только для отображения через `.tr()`. Так как реальных пользователей ещё нет, миграция существующих Hive-записей не требуется физически, но паттерн важно заложить правильно один раз, а не чинить постфактум.

**Не проверено визуально — блокировка на уровне хоста, не кода.** Дважды пытались поднять эмулятор: `Pixel_7a_API_35` завис 9+ минут без `boot_completed` (cold boot), переключились на `Pixel_Fold_API_35` (по эскалационной процедуре) — тот поднялся, но только через ~20 минут при `uptime` load average 20-57 (норма для 8 ядер — около 8). После установки APK (188с на Gradle, 102с на install — тоже многократно дольше обычного) приложение получило ANR ("RIVA PSY isn't responding"), затем `flutter run` потерял соединение с устройством, а следующий скриншот показал уже **системный** ANR — `Process system isn't responding` — то есть завис не только наш процесс, а `system_server` самого эмулятора. Это однозначно перегрузка хост-машины целиком, не связано с переведённым кодом. Решение пользователя: код считается достаточно проверенным через `git diff` построчно + `flutter analyze` (0 ошибок) + полную построчную сверку оставшейся кириллицы; визуальную проверку этих экранов отложить на следующую сессию, эмулятор сегодня больше не поднимать.

**Побочная находка — временная потеря доступа к файловой системе.** В середине сессии инструменты чтения/записи файлов (и даже `ls`/`cat`/`git status` через Bash) стали падать с `Operation not permitted` конкретно на `~/Documents`, `~/Desktop`, `~/Downloads` (сама `~/` при этом листалась нормально) — классический симптом отозванного macOS TCC-разрешения ("Файлы и папки"/Полный доступ к диску) для процесса терминала, видимо связанный с оборвавшимся диалогом разрешения чуть раньше в сессии. Пользователь восстановил доступ через Системные настройки; после этого `ls ~/Documents`/`git status` снова заработали, и в рабочем дереве уже обнаружились результаты правок, сделанных до сбоя (не потеряны). Стоит иметь в виду на будущее: если Read/Edit/Bash вдруг начинают массово падать с `Operation not permitted` на пользовательские папки — это, скорее всего, не баг инструмента, а отозванное системное разрешение, требует действия пользователя в Системных настройках, не решается повторными попытками с моей стороны.

### 39. Key/value-рефакторинг для 5 сущностей (эмоции/события/места/люди/части тела) — начато, 2026-07-26

По итогам архитектурной разведки в §38 (все 5 `repository.dart` хранят русский текст как единственный идентификатор — и для отображения, и для сравнения/поиска/группировки) стартовал сам рефакторинг. Согласованный с пользователем порядок: **модели → Люди → События → Места → Части тела → Эмоции** (Люди и События не читаются экраном "Графики" вообще — безопаснее всего для обкатки паттерна первыми; Эмоции — самые сложные и объёмные, последними).

**Паттерн (закладывается один раз, переиспользуется на каждой сущности):**
- `EventModel`/`BodyPartsModel` получили новое **опциональное** поле `key` (плюс геттер `identity => key ?? name`), а не обязательный — это сохраняет обратную совместимость со ~150 существующими местами создания `EventModel(...)` по всему приложению без необходимости их трогать. Для кастомных записей, которые пользователь вводит сам через "Добавить...", `key` остаётся `null` — сравнение естественно падает обратно на `name`, как и было раньше (это единственный намеренно неполный случай схемы, решение пользователя — не рефакторить дальше).
- В каждом `repository.dart`: seed-список получает `key` на каждую запись, `name` становится `.tr()`-переводом ключа; `updateEvent()` (метод пересохранения в Hive) обновлён, чтобы не терять `key` при каждом ре-сейве.
- В каждом `controller.dart`: сравнение "уже выбрано" (`contain()`) переключено с `.name ==`/`.bodyPart ==` на `.identity ==`. Поиск по тексту (`changeCurrentEventList`) **сознательно остаётся** на `.name` — ищет по уже переведённому отображаемому тексту, это корректно.

**Сделано и закоммичено:**
- Шаг 1 (модели) + **Люди** (`with_who_happened_screen`, 28 записей) — коммит `dcb2f4d`. Все 28 переводов уже существовали в JSON (видимо, тот же предзаготовленный DhiWise-блок, что находили раньше для "Графиков"/"Рекомендаций") — новых ключей не понадобилось.
- **События** (`what_happened_screen`, реально 47 записей, не 46 как оценивали изначально) — коммит (см. ниже). Тоже все переводы уже существовали. 12 из 47 строк в исходнике содержали ручные `\n` для укладки текста в карточку (`EventCard`, `maxLines: 3`, фиксированная ширина) — убраны по аналогии с решением для табов "Графиков"/"Рекомендаций" (положиться на автоперенос). **Не проверено визуально в этой сессии** (эмулятор сегодня не поднимали) — стоит свериться в следующей сессии с рабочим эмулятором, прежде чем считать этот пункт полностью закрытым, особенно для самых длинных фраз ("Компьютерная игра видеоигра", "Просмотр фильма, сериала").

**Места** (`where_happened_screen`, 30 записей — фактически на 1 больше, чем предполагалось при разведке) — все 30 переводов уже существовали в JSON, попутно нашлись и исправились переносом на ключ 2 опечатки в исходнике ("траспорт" → "транспорт", "учереждение" → "учреждение") и одна аббревиатура, развёрнутая в полную форму ("Соц сети" → "Социальные сети"). Единственная сущность из первых трёх (Люди/События/Места), которую читает экран "Графики" — `charts/charts_screen/models/place_model.dart` получил такое же опциональное поле `placeKey`/геттер `identity`, `charts/charts_screen/controller.dart`'s `_getPlaces` переключён на сравнение по `identity` при поиске/создании `PlaceModel`. Внутри `_getPlaces` есть вложенная `listContainEmotion`, сравнивающая **эмоции** (не места) по `.name` — сознательно не тронута, это часть шага "Эмоции". `searchPlaces` (поиск по тексту) тоже не тронут — остаётся на отображаемом имени, как и везде.

**Части тела** (`what_body_parts_screen`, 7 записей) — все 7 ключей уже существовали (те же, что были подключены для `record_add/edit_screen` в более ранней сессии). Единственная из пяти сущностей с расчётом высоты диалога по типу — обнаружено и сведено **шесть**, не три, задублированных места (три `getMessageBoxHeight()`-свитча + три копии `isContain()` в `widgets/body_parts_widget.dart`, по одной на `what_body_parts_screen`/`record_add_screen`/`record_edit_screen`). Расхождение высоты "Плечи и руки" (350 в одном файле vs 314 в двух других) решено по большинству — 314 везде. Высоты централизованы прямо в `core/models/body_parts_model.dart` (`static const _messageBoxHeights` + геттер `messageBoxHeight`, ключ отсутствует для "Спина" — как и было во всех трёх исходных switch, поведение не менялось, просто перенесено); все три `getMessageBoxHeight(model)`-метода в контроллерах теперь однострочники `=> model.messageBoxHeight`, сигнатуры и места вызова не тронуты. `charts/charts_screen/controller.dart` — три места (`_getBodyParts`, `_getBodyPartsType`, `sortBodyPartsList`) переключены на `.identity`; соседняя `item.emotionModel == emotion.name` (сравнение эмоций, не частей тела) в `_getBodyParts` сознательно не тронута — часть шага "Эмоции". У `what_body_parts_screen/controller.dart`, в отличие от Людей/Событий/Мест, вообще нет метода `contain()` — "уже выбрано" для этой сущности живёт только в `isContain()` виджета (уже покрыто выше), это не пропуск, а особенность multi-select экрана.

**Ещё не начато**: `whatHurts`-подпункты частей тела (отдельный лёгкий `.tr()`-проход, решение пользователя — не участвуют в сравнениях, не часть key/value-схемы), Эмоции (+ round-trip строка↔enum через `emotion_in_day_event_extension.dart`/`string_extension.dart`, `charts/controller.dart`'s классификация позитив/негатив/нейтрал через "+"/"-" на конце строки, и `listContainEmotion` внутри `_getPlaces` + `item.emotionModel ==` внутри `_getBodyParts`, обе найдены при работе над Местами/Частями тела).

**Итог рефакторинга на конец шага**: модели → Люди → События → Места → Части тела ✅ пройдены и запушены. Осталась одна сущность — Эмоции (самая объёмная и сложная).

**Решённые по ходу открытые вопросы** (не нужно поднимать снова): расхождение высоты диалога "Плечи и руки" (350 vs 314 в разных файлах) — на усмотрение исполнителя при рефакторинге, не критично для UX; `whatHurts` — отдельный некритичный `.tr()`-проход, не часть этой задачи; существующие тестовые записи в Hive — считаются одноразовым мусором, миграция не нужна.

### 40. Key/value-рефакторинг — Эмоции, последняя сущность (140 записей, 3 списка) — коммит ниже, 2026-07-26

Самый объёмный и содержательный шаг рефакторинга. Все 140 переводов (61+47+32) уже существовали в JSON — включая уже готовую суффиксную конвенцию для нейтральных "+"/"-" пар (`doubt_positive`/`_negative`, часть — `curiosity_+`/`_-`, смешанный стиль, не унифицировал) и позиционное разрешение дубликата "Стыд" (`shame2` первое вхождение, `shame` второе). Новых ключей для самих эмоций не понадобилось — только 4 для категорий-бакетов графиков: `positive_emotions` (новый), `negative_emotions` (уже был), `neutral_positive_emotions`/`neutral_negative_emotions` (новые).

**Архитектура**: `EventModel` получил `isNeutralPositive`/`isNeutralNegative` — геттеры, кодирующие "+"/"-" классификацию по суффиксу `key` (`_positive`/`_+` или `_negative`/`_-`) с fallback на суффикс символа в `name` для кастомных эмоций пользователя (у них `key == null`).

**Round-trip строка↔enum убран полностью, не переименован**:
- `emotion_in_day_event_extension.dart`'s `getEmotionType()` теперь возвращает ключ перевода (`'negative'`/`'positive'`/`'neutral'`), не русский текст — вызывающий код обязан звать `.tr()`.
- `string_extension.dart`'s `getEmotionTypeFromString()` — удалён целиком, никто больше не вызывает.
- **Обнаружено и исправлено**: сама JSON-сериализация `DayEventModel.emotionInDayEvent` в `day_event_model.g.dart` (файл помечен "GENERATED CODE — DO NOT MODIFY BY HAND", но в этом проекте уже правится вручную — судя по нерегулярному форматированию) шла через те же `getEmotionType()`/`getEmotionTypeFromString()`, то есть в Hive писался русский текст, а не стабильное имя enum. Переключено на `EmotionInDayEvent.name`/`EmotionInDayEvent.values.firstWhere(orElse: () => NEUTRAL)` — без выброса исключения на потенциально мусорных старых записях.
- `k27_screen.dart`: `category` стал типизирован как `EmotionInDayEvent` (не `String`) от момента вычисления до сохранения — убран лишний виток enum→строка→enum, который там был. `K27Screen.onSave`'s третий параметр тоже сменил тип.
- `additional_emotions_screen/k31_screen.dart`: `K31Screen.category` — `String?` → `EmotionInDayEvent?`; текст для `controller.title` вычисляется через `.getEmotionType().tr()` в момент присвоения, не хранится и не передаётся как строка.
- Заодно приведён к единому виду `_tabs()` в `k27_screen.dart` (отдельный хардкод-switch для лейблов табов, который в UI-текстовом проходе сознательно не трогали) — теперь тоже через `.tr()` на существующих `negative`/`positive`/`neutral`.

**`charts/controller.dart` — самая широкая правка**: `getEmotionTypesInString`/`getEmotionTypes` переписаны на `isNeutralPositive`/`isNeutralNegative` + `.identity` вместо парсинга символа и сравнения по `.name`, возвращают/используют новые ключи-бакеты вместо русских строк. Все сравнения "та же эмоция" (`_getBodyParts`, оба `listContainEmotion` — в `getEmotions()` и в `_getPlaces`) переключены на `.identity`.

**Побочная находка — `EmotionModel`/`EmotionInBodyModel` тоже нуждались в `key`/`identity`** (не было явно в исходном плане, но необходимо для корректности того самого сравнения на бывшей строке 156, которое план обещал сделать `.identity`): оба получили тот же паттерн, что раньше `PlaceModel` — опциональное поле `key`/`emotionKey` + геттер `identity`, проставляется из `EventModel.key` в момент создания. `what_emotion_widget.dart` — `.name` категорий-бакетов теперь ключ, не готовый текст — добавлен `.tr()` в двух местах (легенда и `xValueMapper` для pie-chart), но **не** в первом `xValueMapper` (строка ~81, использует `emotions`, не `emotionsTypes` — там `.name` уже переведённый текст конкретной эмоции, повторный `.tr()` был бы ошибкой).

**Два найденных бага — сознательно не исправлены, зафиксированы для отдельной задачи**:
1. **Дубль-мутация кастомной нейтральной эмоции** — `what_emotion_screen/controller.dart:81-82` (`updateCurrentEventList`): `listToUpdate.add(model..name += ' +'); listToUpdate.add(model..name += ' -');` — cascade `..` возвращает ту же ссылку на `model`, поэтому в список добавляется **одна и та же** мутирующая ссылка дважды; к концу оба элемента списка — один объект с `name == "X + -"`. Затрагивает только эмоции, добавленные пользователем вручную на нейтральной вкладке (не затрагивает 140 seed-записей).
2. **`else`-баг в классификации тела по категории эмоции** — `charts/charts_screen/controller.dart` (сейчас строки ~134-139, `getEmotions()`): `if (positive) {...} if (negative) {...} else {neutral...}` — `else` синтаксически привязан ко второму `if`, поэтому любая **позитивная** эмоция тоже проваливается в `neutralType!.bodyParts` (не только туда, куда должна); плюс дедуп-проверка внутри идёт по `negativeType!.bodyParts`, а результат добавляется в `neutralType!.bodyParts` — рассинхрон списков. Оставлен явный комментарий `NOTE: pre-existing bug...` прямо в коде на месте.

**Итог рефакторинга**: модели → Люди → События → Места → Части тела → Эмоции — **все пять сущностей пройдены**. `flutter analyze lib/` — 0 ошибок. Не проверено визуально в этой сессии (см. общую политику этого дня — эмулятор не поднимался).

### 41. Фикс бага гейтинга "Обретение" — упражнение "Желания" было доступно без оплаты, коммит `35af643`, 2026-07-26

**Предыстория (по прямому запросу пользователя, факт-чекинг перед этой правкой).** Все три упражнения "Обретения" (Отработать иррациональное, Престиж в фокусе + Три дня без жалоб, Желания) подтверждены реально закодированными и подключены единым `TabBarView` в `working_out_screen.dart` (третье упражнение из ТЗ, "О деньгах, отношениях...", в коде отсутствует полностью — ни файлов, ни следов). Целевая проверка гейтинга (доступ только по тарифу Orion) вскрыла реальный баг: `working_out_irrational_tab.dart:64` и `happiness_in_focus_page.dart:28` содержат рабочую проверку `if (!CurrentUser.tariffIsOrion()) return ... GoToNewTariffWidget(...)`, а `desires_page.dart` — нет вообще. Бесплатные пользователи могли полноценно пользоваться упражнением "Желания" без оплаты.

**Фикс:** в `desires_page.dart` добавлена идентичная проверка тарифа (скопирована один в один с `happiness_in_focus_page.dart:28`, простой вариант без `CounterOfSpentRecordsWidget`, как явно попросил пользователь).

**Попутно решён мёртвый геттер `WorkingOutCubit.showContent`** (`bloc/cubit.dart`) — был определён корректно (`CurrentUser.tariffIsOrion()`), но `grep -rn "\.showContent\b" lib/` подтвердил ноль вызовов где-либо — "ложное ощущение защиты". Решение: не удалён, а подключён как **дополнительный защитный слой** в `working_out_screen.dart` — сразу после `context.read<WorkingOutCubit>()`, до `return Container(...)` с табами, теперь стоит `if (!cubit.showContent) return GoToNewTariffWidget(...)`. Это гейт на уровне контейнера поверх трёх индивидуальных гейтов: именно такой класс бага (один экран внутри `TabBarView` тихо остался без своей проверки) он теперь ловит даже если очередное новое упражнение забудут проверить индивидуально.

`flutter analyze` по всему дереву `working_out/` — 96 существовавших ранее warning/info, ни одной новой ошибки; оба тронутых файла (`desires_page.dart`, `working_out_screen.dart`) чисты (кроме одного preexisting unused-import warning в `working_out_screen.dart`, не связанного с этой правкой). Закоммичено отдельно от локализационной работы, запушено в `origin/pre-pwa-migration-snapshot`.

**Не проверено визуально** (эмулятор не поднимался в рамках этой правки) — стоит визуально подтвердить в следующей сессии с рабочим эмулятором, что бесплатный аккаунт теперь видит пейвол на вкладке "Желания" и что платный аккаунт по-прежнему видит все три вкладки нормально.

**Часть 2 (та же сессия) — проверка случайной смены фоновых картинок для "Желаний".** По ТЗ, у упражнения должны быть дополнительные изображения, которые рандомно меняются (Figma Group 150, id `3167:21`, 16 иллюстраций — мечтательные портреты на фоне еды/аттракционов/сладостей). Проверено: `grep -rn -i "random" lib/.../working_out/desires/` — ноль совпадений; единственный похожий по структуре паттерн, `List.generate(5, ...)` в `wishes_detail_dialog.dart:61`, на деле не рандом, а фиксированный пошаговый онбординг-диалог (5 картинок жёстко привязаны к номеру шага, не выбираются случайно); `assets/images/desires/` содержит 24 файла, ничего похожего по количеству/именам/назначению на 16 портретов из Group 150. **Вывод: фича не реализована вообще, ни в каком виде.**

**Отложено отдельной задачей (не блокирует ничего другого).** Реализовать: (1) экспортировать 16 иллюстраций из Figma (Group 150, id `3167:21`) как ассеты, (2) добавить логику случайного выбора при отображении экрана(ов) "Желаний". **Важное уточнение от пользователя**: эти 16 иллюстраций — ДОПОЛНИТЕЛЬНЫЕ к уже существующим изображениям на экранах "Желаний", не замена им — при возврате к задаче нужно добавлять новый слой картинок поверх текущего дизайна, а не искать, что заменять существующими.

### 42. Локализация всех трёх упражнений "Обретения" (Отработать иррациональное, Престиж в фокусе / Три дня без жалоб, Желания), коммиты `d110089`/`a6ed48a`/`e6e4a2d`, 2026-07-26

По итогам разведки (полная построчная таблица строк по трём деревьям, отправлена пользователю отдельным файлом) применена локализация всех оставшихся хардкод-строк во всех трёх упражнениях "Обретения" плюс табов `working_out_screen.dart`. Каждое дерево — отдельный коммит с диффом на подтверждение перед коммитом (`working_out_irrational/` → `happiness_in_focus/` → `desires/` + табы), по прямому запросу пользователя.

**Comparison risk — сознательно не тронуто.** `working_out_irrational/widgets/relax_dialog/controller.dart:22,25` — Firestore-запрос `.where('name', isEqualTo: 'Мыльный пузырь'/'Стоп мысль')` сравнивает русский текст с полем `name` в коллекции `Audio`. Обернуть в `.tr()` сломало бы совпадение с данными на не-русских локалях. Нужен стабильный `id`/`slug` вместо русского текста в Firestore-документах и коде — отдельная задача, требующая правки данных, не просто кода.

**Новые ключи добавлены (RU/EN/ES) по деревьям:**
- `working_out_irrational/`: `unknown_page`, `back`, `initial_thought`, `initial_action`, `alternative_thoughts_and_actions_title`, `why_i_thought_so` (короткая версия "Почему я так подумал" без абзаца — отличается от уже существовавшего полного `why_i_think_that`, нужна для заголовков таблицы в PDF-отчёте).
- `happiness_in_focus/`: `now_lets_look_at_moments`.
- `desires/`: `report`, `later`, `save_desire`, `desires_stage` (шаблон `"Желания {}/3"`, номер стадии передаётся через `.tr(args: [...])`), `desires_past`, `recommendation_for_efficiency`, `lost` (короткая версия "Пропало" вместо длинного `lost_wish` = "Желание пропало" — для тесного по месту виджета `desire_path_item.dart`).

**Решения пользователя по неоднозначным местам:**
- "Отработать иррациональное" (`working_out_screen.dart`-таб + `initial_working_out_page.dart`) переиспользует уже существовавший ключ `work_out_irrational` = "Проработать иррациональное" — унифицирует формулировку с остальным приложением вместо создания дублирующего ключа с другим глаголом.
- Опечатка со смешанным алфавитом в `happiness_in_focus/super_quest/ui/pages/super_quest_middle_page.dart:93` (`text: 'cохранить'` — латинская `c` вместо кириллической `с`) исправлена заодно с локализацией этого места — теперь `'save'.tr().toUpperCase()`.

**Побочные технические находки:**
- `desires/widgets/wishes_detail_dialog.dart` содержал неразрывные пробелы (U+00A0) и разделители строк U+2028 внутри русских строковых литералов — обычный текстовый `Edit` не матчился по этим местам; правки применены через прямую байтовую замену Python-скриптом.
- `happiness_in_focus/ui/widgets/super_quest_message.dart` — цветной инлайн-счётчик ("Осталось внести N записей", число выделено циан-цветом отдельным `TextSpan`) сохранён через `'remained_fill'.tr().split('{}')` — шаблонный ключ разбивается на пре-/постфикс на месте, а не хардкодится отдельными строками, чтобы стилизация числа осталась независимой от текста.
- Во всех файлах с `import 'package:get/get.dart'` без `hide Trans` добавлен `hide Trans` (established pattern сессии) — избегает коллизии GetX-расширения `Trans` с `.tr()` из `easy_localization`.

**Итог:** все три упражнения "Обретения" полностью локализованы, все три `TabBar`-лейбла `working_out_screen.dart` тоже. `flutter analyze` после каждого дерева — 0 новых ошибок (только уже существовавшие warning/info). Не проверено визуально в этой сессии — эмулятор не поднимался.

### 43. Локализация 4 экранов Настроек (о приложении, профиль, подписка, пароль), коммиты `8c54080`/`992896d`/`156b9bb`/`3a3ce45`, 2026-07-26

По разведке выяснилось, что заявленный объём шире названий: "подписка" — цепочка из 4 экранов (`settings_subscription_screen` точка входа → `settings_buy_subscription_screen` → `settings_tariff_activated_screen`, плюс `settings_promo_screen` отдельной веткой), "пароль" — 2 экрана (`settings_set_password` → `settings_repeat_password_screen`, именно смена пароля из Настроек, не экран входа при логине). Каждое дерево — отдельный коммит с диффом на подтверждение, тот же порядок работы, что и для "Обретения" (§42).

**Comparison risk — сознательно не тронуто.** `core/models/tariff_model.dart`'s поле `TariffModel.name` (значения `'Орион'`/`'Базовый'`) используется одновременно как отображаемый текст и как бизнес-идентификатор: сравнение `!=`/`==` в `settings_subscription_screen/k13_screen.dart:77` (`CurrentUser.user.currentTariff!.name != 'Базовый'`) и `settings_tariff_activated_screen/repository.dart:20-21`, персистентность в Hive через `toJson()`. Тот же паттерн, что раньше чинили для эмоций/событий/мест (§39-40) — нужно отдельное display-only поле (модель уже содержит `nameInEn`, подсказывающее направление), не трогать `.name` напрямую. Единственное место, где динамическое имя тарифа реально входило в UI-строку (`settings_buy_subscription_screen/k14_screen.dart:293`, `"Перейти на тариф \"Орион\""`) — переписано как `'${'go_to_tariff'.tr()}"${tariff.name}"'.toUpperCase()`, вместо того чтобы зашивать "Орион" в новый ключ перевода — строка-идентификатор тарифа остаётся вне словарей.

**Мёртвый код, исключён из работы**: `settings_payment_subscription_screen/` — grep по всему `lib/` подтвердил, что ни один экран на него не переходит (как в прошлой сессии с третьим упражнением "Обретения"). Внутри `settings_buy_subscription_screen/k14_screen.dart` — ветка `newBody()` (`return true ? old(context) : newBody(context);`) тоже мертва; строки в ней идентичны байт-в-байт активной ветке `old()`, поэтому обычный `Edit`-инструмент не мог их различить — точечная замена сделана по номерам строк через Python-скрипт, чтобы гарантированно не задеть мёртвую ветку.

**Отложено отдельной задачей**: весь текст Условий использования и Политики конфиденциальности в `about_app_screen/controller.dart` (252 строки, ~30 секций) — в JSON уже существует почти полный набор соответствующих ключей (`terms_of_use`, `introduction_content`, `general_conditions_content` и т.д.), но код их нигде не использует, нужен отдельный построчный проход на точное сопоставление, слишком объёмно для этой задачи.

**Побочные находки:**
- Задублированный в 3 разных файлах (не переиспользуется, скопирован) валидатор пароля и его строки ошибок: `settings_profile_screen/widgets/retractable_container_widget.dart`, `initial_setup/sign_in/presentation/sign_up_screen/k2_screen.dart`, `initial_setup/sign_in/presentation/reset_password_screen/reset_password_screen.dart`. По решению пользователя — только общий `.tr()`-ключ на все три места (`password_length_min`/`password_length_max`/`password_complexity`, заведены в этой сессии), без рефакторинга самой функции-валидатора; переиспользование в двух других файлах — отдельная лёгкая задача на будущее.
- Опечатка в уже существующем ключе `passwprd` (вместо `password`) — по решению пользователя оставлена как есть, чтобы не рисковать пропустить использование где-то за пределами разведанных экранов.
- Одна и та же сетевая ошибка существовала с двумя разными опечатками в двух файлах (`settings_profile_screen/controller.dart` — "интрнету"/"позже", `settings_tariff_activated_screen/k17_screen.dart` — "интернету"/"позднее") — консолидированы в один ключ `network_error_try_later` с канонической формулировкой.
- Двойные пробелы в паре исходных строк (`"Ввести  промокод"`, `"за  ${...} ₽/год"`) починены при переносе на ключ/шаблон.

**Итог:** `about_app_screen` (без legal-текста), `settings_profile_screen`, вся цепочка "подписка" (4 экрана), вся цепочка "пароль" (2 экрана) — полностью локализованы. Добавлено 44 новых ключа суммарно по всем 4 коммитам (RU/EN/ES). `flutter analyze` после каждого дерева — 0 новых ошибок. Не проверено визуально в этой сессии — эмулятор не поднимался.

### 44. Локализация онбординг-цепочки после первого входа, коммит ниже, 2026-07-26

Диалоги показываются по одному через `main_screen/controller.dart`'s `openMessages()`, в приоритетном порядке (проверка через `SharedPreferences`): `K3Screen` (set_reminders) → `SendPushesScreen` → `RecommendationBuyTariffScreen` (это "тариф") → `PillRemindersScreen` → уже локализованное ежемесячное напоминание про частоту уведомлений (не в этой задаче) → `K4Screen` → `TryIrrationalDialog` (уже локализован, вне задачи по прямому запросу пользователя).

**🐛 Найденный баг — зафиксирован, не исправлен (по решению пользователя).** `main_screen/controller.dart`, условие показа `K4Screen`: `if (false ?? Random().nextInt(100) > 50 && !CurrentUser.usedOreonTrials && !CurrentUser.tariffIsOrion())`. В Dart оператор `??` вычисляет правую часть только если левый операнд — `null`. Литерал `false` никогда не `null`, поэтому правая часть никогда не вычисляется, и всё условие всегда равно `false`. **Диалог `K4Screen` не показывается ни одному пользователю ни при каких обстоятельствах** — не редкое мелькание (как можно было бы подумать из-за `Random().nextInt(100) > 50`, то есть "50% шанс"), а полная недостижимость кода. Похоже на опечатку разработчика (видимо, задумывался обычный `if (Random()...)`, либо временный флаг для отключения, который стал постоянным). Из-за этого `k4_screen.dart` **не локализован** в рамках этой сессии — по решению пользователя нет смысла переводить строки, которые никто не увидит; при будущем исправлении бага потребуется отдельный проход по этому файлу.

**Локализовано** (4 живых файла): `set_reminders_screen/k3_screen.dart`, `send_pushes_screen/send_pushe_screen.dart`, `recomendation_buy_tariff_screen/recomendation_buy_tariff_screen.dart`, `pill_reminders/pill_reminders_screen.dart`. `set_reminders_screen/controller.dart` — проверен, кириллицы не содержит, не редактировался.

**Сознательно не найдено проблем с идентичностью данных.** Разведка целенаправленно проверяла паттерн "русский текст как идентификатор" (уже дважды всплывавший в этой сессии — эмоции/тарифы, см. §39-40, §43) во всех 4 файлах: выбор количества напоминаний в `k3_screen.dart` идёт по полю `int quantity`, все `SharedPreferences`-ключи (`set_reminders`, `send_pushes`, `recommendation_buy_tariff`, `pill_reminders`) — английские строки. Риска не найдено.

**Новые ключи**: `tariff_title` ("Тариф"), `for_storing_data_go_to_orion` (длинный текст про переход на тариф Орион).

**Переиспользовано** (17 мест): `notifications`, `times_per_day`, `change_time`, `in_settings`, `you_can_refuse`, `continue`, `allow_for_app`, `no`, `yes`, `go_to_tariff` (+ конкатенация буквального "Орион", тот же паттерн, что уже применялся в `settings_buy_subscription_screen/k14_screen.dart`, §43), `in_free_version` (заодно чинит опечатку в исходнике — "хранится" без мягкого знака → корректное "храниться" из ключа), `later`, `reminders`, `setup`, `subscription`, `subscribe`.

**Попутная синхронизация формулировки**: ключ `can_set_reminders_time` в `ru-RU.json` обновлён — было "в Настройках... о приеме..." (без ё) через точку-пробел между предложениями, стало "в Настройках... о приёме..." (с ё) через перенос строки `\n` — приведено к более грамотному и визуально идентичному варианту из исходного кода `pill_reminders_screen.dart`. EN/ES-версии не трогались (там нет аналогичной проблемы).

`flutter analyze` — 0 новых ошибок. Не проверено визуально — эмулятор не поднимался в этой сессии.

### 45. Локализация модуля Pills (экраны препаратов), коммит ниже, 2026-07-26

8 файлов из 16 в дереве `settings_pills/` содержали кириллицу: `settings_pills/controller.dart`, `settings_pills/settings_pills_screen.dart`, `settings_pills/widgets/pill_card_widget.dart`, `settings_pills_add_bottom_sheet/controller.dart` + `.dart`, `settings_pills_calendar/settings_pills_calendar_screen.dart`, `settings_pills_edit_bottom_sheet/controller.dart` + `.dart`. Остальные 8 (модели, `pill_model.g.dart`, `repository.dart` и др.) — чистая логика без UI-текста, проверены и не тронуты.

**Comparison risk — не найдено.** Целенаправленно проверили `PillModel`/`AdoptionModel` на предмет русских enum-подобных полей (частота приёма, "до/после еды", единицы измерения) — в модуле их нет вообще: `name` — свободный пользовательский текст, `hoursOfTakingPills` — свободные строки времени в формате `HH:MM`, введённые через `TextFormField` с маской, не выбор из русского списка. Весь UI-текст переведён без архитектурных оговорок.

**Починены 2 опечатки в существующих ключах** (во всех трёх языковых файлах, RU/EN/ES): `add_appointment` — добавлена пропущенная буква ё ("Добавить время прие**м**а" → "прие**ё**ма"); `delete_reminder` — убрано лишнее слово "и", не соответствовавшее реальному тексту в коде ("Убрать напоминание **и** из актуальных..." → "Убрать напоминание из актуальных..."). EN/ES-версии этих двух ключей тоже приведены в соответствие по смыслу.

**Дублированная логика, сведена к одному ключу.** Функция `getDurationText()` (fallback-текст "установить длительность приема") была скопирована в 3 разных файлах (`pill_card_widget.dart`, `settings_pills_add_bottom_sheet/controller.dart`, `settings_pills_edit_bottom_sheet/controller.dart`) — все три теперь используют один ключ `set_reception_duration`.

**15 новых ключей**: `add_pill`, `reminder_n` (шаблон с номером через `.tr(args:...)`), `add_pill_dialog_title`, `fill_all_fields`, `enter_medication_name`, `edit_medication_name`, `medication_name_hint`, `enter_medication_name_full`, `edit_abbrev`, `cancel_noun` (сознательно отдельно от уже существующего `cancel`="Отменить" — разные слова, "отмена" vs "отменить", не объединять), `appointment_reminder_singular` (отдельно от множественного `apoinment_reminders`), `edit_reminder_title`, `add_reminder_to_actual`, `cancel_intake`, `resume_intake`.

**Переиспользовано** (~19 мест): `date_added`, `current_appointments`, `settings`, `apoinment_reminders`, `back`, `set_reception_duration`, `redo`, `set_reception_time`, `save`, `add_appointment` (после починки), `select_start_period`, `select_end_period`, `delete_reminder` (после починки), `cancel`.

`flutter analyze` — 0 новых ошибок. Не проверено визуально — эмулятор не поднимался в этой сессии.

### 46. Локализация технических экранов уведомлений/сообщений (предложения, ошибки, напоминания), коммит ниже, 2026-07-26

Три модуля из Settings-хаба: `settings_offers_screen` ("Предложения по улучшению"), `settings_errors_screen` ("Сообщить об ошибке"), `settings_reminders_screen` (настройка времени напоминаний о создании записи — отдельно от уже переведённого в §45 модуля Pills с напоминаниями о приёме лекарств). `send_pushes_screen` не трогали — уже переведён в онбординге (§44).

**Comparison risk — не найдено.** Целенаправленно проверили `settings_reminders_screen` (планирование времени напоминаний) на дни недели или другие русские enum-подобные значения — их нет: выбор количества напоминаний идёт по `K12Model.quantity` (`int`), `K12NotificationModel.title` (русский текст вида "Напоминание №N") нигде не сериализуется и не сравнивается — участвуют только `hour`/`minutes` в формате "HH:MM", которые реально сохраняются через `CurrentUser.repo.setLocalUserData(reminderTimeInStr: ...)`.

**Отдельно проверена (по прямому запросу пользователя) логика, не связанная с переводом**: куда реально уходят данные с экранов "Предложения" и "Ошибка" — совпадение hint-текста "Ваши предложения" на обоих экранах могло быть признаком того, что сами данные путаются, а не просто UI-копипаст. Проверено чтением `settings_offers_screen/controller.dart` и `settings_errors_screen/controller.dart`: экран предложений пишет в Firestore-коллекцию `'Offers'`, экран ошибок — в `'Errors'`, полностью раздельно. Подтверждено: это чистый UI-копипаст, данные не путаются. Hint-текст "Ваши предложения" оставлен как есть на обоих экранах (общий шаблон, по решению пользователя, раз данные разделены корректно).

**Решение по формулировке сетевой ошибки**: на экранах "Предложения"/"Ошибка" была своя формулировка "Ошибка, попробуйте позже или проверьте подключение к интернету" — не опечатка, а другой порядок фраз по сравнению с уже существующим `network_error_try_later` = "Произошла ошибка, проверьте подключение к интернету или попробуйте позже" (заведён в §43 для профиля/подписки). По решению пользователя — унифицировано на существующий `network_error_try_later`, отдельный ключ не заводили.

**Конфликт с ключом из модуля Pills**: в Pills (§45) уже существует `reminder_n` = "Напоминание {}" (без знака №). Здесь тот же паттерн, но с знаком №: "Напоминание №{}". Заведён отдельный ключ `reminder_no_n`, существующий `reminder_n` не трогали, чтобы не сломать Pills.

**4 новых ключа**: `suggestion_title`, `suggestion_sent` (переиспользован дословно на обоих экранах — предложения и ошибки, как и было в исходном коде), `error_message_title`, `reminder_no_n` (шаблон с номером через `.tr(args:...)`).

**Переиспользовано** (~16 мест): `settings`, `write_your_wishes`, `your_suggestions`, `upload_screenshot`, `upload_image`, `send`, `reminders`, `times_per_day`, `you_can_refuse`, `remind_to_make_entry`, `save`, `change`, `report_an_error`, `describe_issue`, `error_screenshot`, `network_error_try_later`.

`flutter analyze` — 0 новых ошибок. Не проверено визуально — эмулятор не поднимался в этой сессии.

### 47. Разведка + фикс: сервер не хранит содержимое дневника (СМЭР), коммит `72f238b`, 2026-07-26

**Контекст.** Принято архитектурное решение — минимизировать доступ сервера к данным пользователя: дневник должен храниться только на устройстве, бэкап — ответственность пользователя (собственный Google Drive), не автоматическая синхронизация с сервером.

**Разведка (сначала только чтение, без правок) подтвердила: так уже и есть.** Дневниковые записи (`DayEventModel` через `K49Repo`) хранятся исключительно в Hive локально — проверено исчерпывающе: все потребители `K49Repo` (`records_screen`, `record_add_screen`, `record_edit_screen`, `charts_screen`), полный `FireStoreRepositoryImpl` прочитан целиком, финальный `grep DayEventModel` пересечённый с `firebase|firestore` по всему `lib/` — ноль совпадений. **Сервер физически не имеет доступа к содержимому дневника.**

Бэкап через `google_drive_service.dart` тоже уже реализует нужную модель: JSON-снимок Hive-базы заливается в **собственный** Google Drive пользователя (через его же Google-аккаунт, `GoogleSignIn`+`driveFileScope`), в Firestore при этом пишется только `BackupModel` — служебный указатель (`file_ID` на Drive, `date_time`, `record` — строка-счётчик вида "Записей: N", `service`, `file_name`), **без содержимого записей**. Восстановление идёт по обратной цепочке: список бэкапов — из Firestore (только метаданные), сам файл — с Google Drive пользователя.

**Найдено реальное расхождение UI и кода**, не архитектурная проблема: экран "Данные и восстановление" утверждал "Ваши данные будут автоматически копироваться... каждую неделю" — это было **неправдой**. `createServiceBackup()` вызывается только по нажатию кнопки "Резервная копия"; блок в `main_screen/controller.dart`'s `openMessages()`, похожий на еженедельную проверку, на деле только обновлял таймстемп `serviceCopyData`, не создавая бэкап; регистрация периодической WorkManager-задачи давно закомментирована. Автоматического бэкапа в коде не было вообще.

**Исправлено (3 файла):**
1. `data_and_recovery_screen.dart` — текст заменён на формулировку, отражающую реальность (ручной бэкап по кнопке "Резервная копия" в любой момент).
2. Убран декоративный тумблер "Вкл./Выкл." на том же экране — переключал `serviceEnable`, который ничего не активировал/деактивировал в реальности. Следом вычищены ставшие полностью мёртвыми `serviceEnable`/`servicePrefsKey`/`changeServiceEnable()` из `DataAndRecoveryController`.
3. Удалён мёртвый блок еженедельной проверки в `main_screen/controller.dart:openMessages()`, ссылавшийся на удалённый `serviceEnable`.

**Явно НЕ тронуто** (по прямому уточнению пользователя, чтобы не перепутать с похожими по смыслу, но функциональными элементами): круговые регуляторы фиксации состояния на главном экране (питают график "Диагностика состояния") и в конце "Пути" (интенсивность эмоции) — полностью рабочие элементы, не связаны с удалённым тумблером бэкапа.

**Смежная, более крупная находка — зафиксирована, не в этой задаче.** `FireStoreRepositoryImpl` целиком показывает, что сервер сейчас хранит заметно больше "email + подписка": `login`, возраст (`old`), пол (`male`), MD5-хеш пароля (`UsersData`, слабый алгоритм хеширования — отдельная заметка), свободный текст обращений (`Offers`/`Errors` + опциональный скриншот в Storage), тексты ошибок (`Exceptions`). Это отдельная, архитектурно более крупная задача (сравнимая по масштабу с key/value-рефакторингом §39-40), если решат за неё браться — не смешивается с задачей про дневник.

`flutter analyze` — 0 ошибок. Не проверено визуально — эмулятор не поднимался в этой сессии.
