import 'package:flutter/material.dart';

class WorkingOutPage extends StatelessWidget {
  const WorkingOutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
